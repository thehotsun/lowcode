# lowcode 移动端列表第一期实现契约

> 本契约已按《移动端列表五期整体方案.md》重写，范围与其 1.9 节第一期一致，验收标准与其 1.10 节一致。各能力域的完整语义细则（按钮、搜索、选择、外部 API 全集）见《移动端列表渲染方案.md》对应章节，按五期方案分期实施。

## 1. 契约目的

本契约用于指导 lowcode 移动端列表渲染器第一期的开发、联调和验收。

第一期目标是：在不影响桌面端的前提下，为 `pageLayout: "table"` 提供卡片式移动端运行时；设计端新增与 `tableAttrs` 平级的顶层移动端配置 `mobileAttrs`（operate.vue 独立“移动端设置”入口 + 独立属性弹窗）；移动端渲染单列卡片列表，点击卡片通过 JS 在父级容器内打开详情页。

第一期不做：搜索/筛选/排序（第四期）、操作列与按钮（第五期）、选择与批量（第三期）。

移动端渲染器由 `complete-table` 内部集成：宿主 `tableRender` 通过 `provide` 提供 `isMobile`（CommonDP utils 的 `isMobile()` UA 判定），`complete-table` 经 inject 读取后内部切换移动端/桌面端渲染，对外始终只挂载 `complete-table` 一个组件；移动端渲染器只依赖 lowcode 现有配置协议、运行时能力和 `mobileAttrs`。

## 2. 第一期开通范围

### 2.1 必须支持

设计端：

- 顶层 `mobileAttrs` 配置（独立键）：标题键、移动端展示列、卡片单/双列、label 上下/左右。
- operate.vue 独立“移动端设置”按钮 + 独立属性弹窗（`mobileAttrsDlg.vue`）；保存、回显、导入导出兼容。
- 设计端加载与渲染端解析均按 `merge({}, getMobileAttrs(), 保存值 || {})` 合并：旧 JSON 无该键取默认值，未来新增配置字段旧页面自动补默认值，不失效、无需迁移。

渲染端：

- 单列卡片列表：标题行（仅标题键，未配置不展示）+ 单/双列字段网格 + label 上下/左右。
- `Boolean(show) === true` 的列显隐语义，卡片渲染列 = `mobileFields` ∩ 显示叶子列。
- 格式化渲染：普通字段、字典（含 `dicCode` 运行时请求与缓存、`contentStyle` 颜色胶囊）、人员（首字母头像）、`contentTextAttrArr` 样式（条件片段、文本、样式、图标）、复杂自定义 `formatter`（返回组件定义，`renderMobileField` 上下文）。
- 点击卡片打开详情页：JS 铺满父级容器、label 左右展示全部显示字段、上一条/下一条 pager、不请求详情接口。
- 分页、触底加载、手动“加载更多”、`requestVersion` 并发治理、失败恢复。
- 生命周期 `onInitEvent` / `onBeforeQueryDataEvent` / `onAfterQueryDataEvent`。
- 最小外部 API：`init`、`expose_preview`、`expose_refreshData`、`expose_getTableData`、`expose_setTableData`；父组件 `rowClick` 事件。
- 预览态、禁用态、空态/加载态/错误态。

### 2.2 明确不支持

以下能力第一期不得渲染入口，也不得自行修改请求协议；对应配置保持原样，不影响桌面端：

- 搜索区域、筛选工具条、排序（第四期）。
- 操作列与一切按钮入口（含新增、刷新、浮动按钮、字段点击 `clickEvent.relateBtnId`、`dbClickRelateBtnId` 关联动作）（第五期）。
- 卡片选择、单选/多选、跨页选择、批量操作、FreeLayout 事件全集、`expose_*` 全集（第三期）。
- `tabs-table`、`tree-table`、多级表头分组、`showSummary`/`summaryMethod`、二维码、打印导出、导入、关联自定义组件、`isMerge`/`spanMethod`、会话级列设置面板、远端排序/远端列筛选。

## 3. 输入配置契约

移动端入口接收桌面端同结构 JSON，另含与 `tableAttrs` 平级的独立顶层键 `mobileAttrs`：

```js
{
  pageLayout: "table",
  tableOptions: [],
  formOptions: [],
  tableAttrs: {},
  mobileAttrs: {
    titleField: "",         // 标题键，fieldCode；空则标题行不展示
    mobileFields: [],       // 移动端展示列，fieldCode 有序数组
    fieldLayout: "double",  // single | double
    labelLayout: "vertical" // vertical | horizontal
  },
  fuzzyFieldSearchConfig: {},
  keyField: ""
}
```

合并规则（前向兼容，设计端与渲染端一致）：

```js
mobileAttrs = merge({}, getMobileAttrs(), json.mobileAttrs || {})
```

旧 JSON 无 `mobileAttrs` 时取全部默认值；未来新增配置字段时旧页面自动补默认值、不失效；未知字段忽略。

字段规则：

- 只处理 `show` 为真值的叶子字段；含 `children` 的父节点不渲染。
- 字段顺序与 `tableOptions` 一致；字段名使用 `fieldName`，取值使用 `fieldCode`，兼容返回数据字段名大小写差异。
- `keyField` 用于记录唯一身份（第三期跨页选择使用，第一期先行兼容处理）。

`mobileAttrs` 回退规则（旧 JSON 无 `mobileAttrs`、或字段为空时按序回退，不阻断渲染）：

1. `titleField` 为空 → 标题行不展示（不回退到任何字段），字段区照常渲染。
2. `mobileFields` 为空（或全部项均为失效 fieldCode 被跳过）→ `tableOptions` 中前 4 个 `show` 为真值的叶子列（已用作标题的剔除；不足 4 个时展示现有的全部）。
3. `fieldLayout` / `labelLayout` 非法值 → 默认 `double` / `vertical`。

校验规则：`mobileFields`、`titleField` 必须是显示叶子列的 `fieldCode`；设计端保存时剔除失效项，渲染端遇失效项跳过并输出开发告警。

## 4. 卡片渲染契约

### 4.1 运行状态

```js
{
  currentSelectedRow: Object | null, // 卡片点击时更新
  detailVisible: boolean,            // 详情页是否打开
  detailIndex: number,               // 详情页当前记录在已加载列表中的下标
  loading: boolean,                  // 首查/替换式查询进行中
  loadingMore: boolean,              // 加载更多进行中（单飞）
  loadError: boolean,                // 首查失败标志：空态错误页与重试按钮
  loadMoreError: boolean,            // 加载更多失败标志：页脚错误与重试按钮
  detailLoadingMore: boolean,        // 详情页pager自动加载进行中（pager loading态）
  finished: boolean,                 // 计算属性（!hasMore）：无更多数据
  pageNo: number,
  pageSize: number,
  totalCount: number,
  requestVersion: number,            // 并发版本号，拒旧响应
  // 搜索参数状态（与web端getParams同构；第一期无搜索UI，值为空默认值，第三期UI接入后生效）
  searchForm: Object,
  searchDateRangeFields: Array,      // 日期范围字段登记（请求时拆分Start/End）
  searchFormValueParsers: Array,     // 数值范围字段转换器（请求时parseValue）
  // 字典缓存（按dicCode复用）
  dictCache: Object
}
```

### 4.2 卡片结构

- 标题行：`titleField` 单独一行渲染，走格式化渲染，不再重复出现在字段区；未配置 `titleField` 时标题行不展示，不回退其他字段。
- 字段区：按 `mobileFields` 顺序渲染；`fieldLayout` 决定单列或两列网格（两列时先行后列，末尾奇数项占单格半行、不跨整行）；`labelLayout` 决定每个字段是 label 上/值在下还是 label 左/值右。
- 摘要值超长单行截断；完整值在详情页查看。
- `align`、`columnWidth`、`fixed`、`show-overflow-tooltip` 的桌面样式均不生效：移动端不消费列 `align`（web 表格单元格语义），字段值固定居左，排布只受 `labelLayout` 影响。
- `renderHeader` 不执行，字段标签使用 `fieldName`。

### 4.3 点击

- 点击卡片任意区域：打开详情页、更新 `currentSelectedRow`、`$emit("rowClick", row)`。
- 预览态和禁用态不发布业务事件。
- 字段点击第一期不响应；第五期按钮体系接入后字段点击优先于卡片点击并阻止冒泡。

## 5. 详情页契约

- 呈现：JS 控制，以 `position: absolute; inset: 0` 铺满列表父级容器，不走路由、不使用全屏 dialog；父容器未设置 `position: relative` 时组件自动补设；宿主需为容器提供确定高度。
- 自带导航栏（返回按钮 + “记录详情”）；返回后列表数据与滚动位置不变，不重新请求。
- 标题区：`titleField` 渲染结果（大标题，走格式化渲染）；未配置标题键则标题区不展示。
- 字段行：全部 `show` 为真值的叶子字段，固定 label 左、值右布局（不受 `mobileAttrs.labelLayout` 影响）；值超长换行完整展示；行间细分隔线。
- 格式化渲染与卡片使用同一字段渲染器。
- pager：切换范围 = 当前已加载记录，显示 `当前序号/totalCount`（总数为服务端 `totalCount`，非已加载数；一次性加载时两者相等）与上一条/下一条；到达首条时“上一条”按钮置灰；点击“下一条”到达已加载末条且 `finished === false` 时，自动触发加载更多请求（复用列表同一加载方法与并发约束：单飞、`requestVersion` 校验），成功后进入新记录，加载中在 pager 上显示 loading，失败给出提示并可重试、停留当前记录。
- 数据来源：当前行数据，不请求详情接口。
- 预览态允许查看详情页（纯展示，不发布事件）。

## 6. 字段渲染契约

渲染优先级固定为（`formatter` 分派见 `BaseRenderTable/index.js:798-810`，其余见 `getCellRender` `:191-264`；各层互斥、命中即止）：

```text
formatter > contentTextAttrArr > PERSON > DICT > 原始值
```

（桌面代码中另有 `slotName` 插槽、`tagName` 程序化组件渲染两层，因设计器保存的 JSON 不会产出这两个字段，移动端渲染链中省略。）

格式化渲染由字段配置驱动：有 `contentTextAttrArr`、`cellRenderType` 等配置才启用对应渲染，无配置展示纯文本。该规则对卡片字段区、卡片标题行、详情页标题区与字段行一致——凡有格式化配置的位置一律优先格式化渲染。

### 6.1 `contentTextAttrArr`

- `conditionalJudgment` 以列表实例为 `this` 执行；异常时隐藏当前片段并输出警告。
- `textVal || cellValue`；`textVal` 支持 `{}` 占位符模板解析（与桌面端一致）：`{}` 取当前单元格的值，`{fieldName}` 取行数据中对应字段的值（如 `第 {} 次变更`、`最后修改人：{updateUserName}`），未匹配的字段替换为空串（行数据取值按大小写不敏感回退）。
- `isBold`、`isItalic`、`isStrikethrough`、`isUnderline`、`color`、`textStyle`。
- `iconName`、`iconPosition`、`iconStyle`。
- 多片段按配置顺序渲染。
- `clickEvent.relateBtnId` 第一期不触发（第五期接入）。

### 6.2 字典

- `enumDisplayConfig.dicList` 匹配：`dicId === String(cellValue)`；未匹配回退原始值。
- `cnName` 作为显示文本；`contentStyle` 的 `backgroundColor`/`color` 渲染为圆角胶囊标签，无背景时纯文本。
- 仅配置 `dicCode` 时请求 `/dic/content/list?dicCode=...` 并按 `dicCode` 缓存复用。
- 预览态不请求远程字典。
- 请求失败时显示原始值并记录错误，不阻断列表。

### 6.3 人员

- `cellRenderType === PERSON` 且值为字符串：首字母头像 + 名称。

### 6.4 复杂 `formatter`

第一期必须支持复杂自定义 `formatter`，通过移动端字段渲染上下文执行：

```js
renderMobileField({ row, column, index, mode: "card" | "detail", mobileMode: true })
```

- `str2Fn` 执行返回组件定义（`components`/`data`/`computed`/`watch`/`methods`/`template`），移植桌面 `Vue.extend + Vue.compile` 机制（`BaseRenderTable/index.js:745-841`）。
- `provide` 与桌面一致的能力：`getTableRenderInstance()`、`emitBtnClick(row, btnId)`；`emitBtnClick` 第一期提供注入入口，完整执行依赖第二期 `executeButton` 抽取，触发时输出开发告警。
- 不为依赖 Element Table 专有 API 的历史 formatter 提供兼容代理（2026-09-11 决策）：渲染机制与 web 端 `BaseRenderTable` 保持一致，此类脚本执行/编译异常时开发告警并回退该字段后续链路（内容文本/字典/人员/原始值），不得导致整卡/整列表渲染失败。
- 所有现有复杂 formatter 的验收口径：渲染机制与 web 端 `BaseRenderTable` 保持一致即视为满足，真实环境逐个回归随宿主联调进行，不作为交付阻塞项。

## 7. 数据加载与并发契约

### 7.1 加载规则

- `showPagination === true` 时使用 `pageNo`、`paginationSize` 分页请求；`false` 时一次性加载。
- 首查、刷新、外部参数变更：替换列表并回到第一页。
- 加载更多：递增页码并追加列表。
- `totalCount` 判断是否还有更多数据。
- 触底（IntersectionObserver 哨兵）与“加载更多”按钮共用同一个加载方法。
- 查询参数与桌面 `getParams` 同构（键与合并顺序一致）：`searchForm` 空默认值（含数值范围 `Start/End` null 键、多选/日期范围空数组）、日期拆分 `fieldStart/fieldEnd`、数值范围 `parseValue` 转换均已按同语义实现（搜索字段不区分桌面"列表上方/表头下方"摆放，统一一处处理）；第一期无搜索 UI，值为空默认值，第三期接入 UI 后直接生效；`multiFieldSearch` 恒为空；`advSearchExpr`/`advSearchExprJoinOp` 恒为默认值（`[]`/`"and"`）；`page` 形状与桌面一致（`pageNo`/`pageSize`/`totalCount`）；非项目路由删除 `prjId`。

### 7.2 并发规则

- 首查/刷新类请求使用递增 `requestVersion`，旧响应不得回写列表、总数或状态。
- 同一时间只允许一个加载更多请求；重复触底直接忽略。
- 加载更多失败时恢复页码、保留已有卡片、允许重试。
- 组件销毁后禁止异步回写；销毁时取消哨兵观察。
- 所有 loading 状态必须在成功、失败和取消路径复位。

## 8. 对外 API 与事件契约（第一期最小集）

```js
init(isPreview, json, options, externalTriggerQueryTableData, tableDisbaled)
// 第三参形状与桌面端tableItem.init一致：{ externalParams, dynamicExternalParams }
expose_preview(data)
expose_refreshData(data, dynamicData)
expose_getTableData()
expose_setTableData(data)
```

参数语义：

- `expose_refreshData(data, dynamicData)`：`data` 合并持久化外部参数；`dynamicData` 替换动态外部参数。
- `expose_setTableData(data)`：直接替换本地列表数据并更新总数。
- `expose_preview(data)`：进入预览态，不请求字典和远程选项，不执行生命周期与查询（以本地样例数据渲染）。

事件：父组件 `rowClick(row)`（卡片点击时发布）；预览态、禁用态不发布。

其余 `expose_*` 方法与 FreeLayout 四类事件在第三期对齐，语义清单见《移动端列表渲染方案.md》§7、§8。

## 9. 生命周期契约

```text
解析 JSON
  -> 组合字段与 mobileAttrs 回退
  -> onInitEvent
  -> 组装最终查询参数
  -> onBeforeQueryDataEvent(params)
  -> 请求数据
  -> onAfterQueryDataEvent(params, res.data)
  -> 首查替换 / 加载更多追加
```

- `onInitEvent` 在配置解析和运行数据组合完成后、首次查询前执行。
- `onBeforeQueryDataEvent(params)` 同步返回 `false` 时取消请求。
- `onAfterQueryDataEvent(params, res.data)` 仅在成功响应后执行。
- 生命周期函数以移动端列表实例作为 `this`。
- 生命周期异常必须记录错误，不能破坏组件卸载和加载状态复位。
- `onResetBtnEvent` 第四期随搜索接入。
- 预览态（`init` 的 `isPreview` 或 `expose_preview`）不执行生命周期钩子、不发起查询，仅以本地样例数据渲染。

## 10. 验收标准

### 10.1 配置

- 可配置标题键、展示列、单双列、label 布局；保存后顶层 `mobileAttrs` 正确且独立于 `tableAttrs`；重新打开设计器回显正确（加载时与默认值 merge）。
- 旧 JSON（无 `mobileAttrs`）打开设计器不报错；移动端按回退规则渲染出完整卡片。
- `show` 为假的列不出现在移动端任何位置；`mobileFields` 含失效 fieldCode 时保存自动剔除、渲染跳过。

### 10.2 卡片

- 卡片为单列列表；标题行 = 标题键单独一行，未配置标题键时标题行不展示；字段区按单/双列、上下/左右正确排布，两列时先行后列。
- 标题键字段不重复出现在字段区。
- 有格式化配置的字段（含标题键）按格式化渲染，无配置的字段展示纯文本；字典胶囊、人员头像、内容文本样式（含条件片段、图标）渲染正确；值超长截断。
- 复杂 `formatter`（返回组件定义）在卡片与详情页均可渲染；依赖 Element Table 专有 API 的历史脚本回退后续链路并输出开发告警；执行失败只影响当前字段。
- 点击卡片打开详情页，同时父组件收到 `rowClick(row)`。

### 10.3 详情页

- 以 JS 覆盖层铺满父级容器（父容器无 `position: relative` 时自动补设），非路由跳转；返回后列表数据与滚动位置不变。
- 展示全部显示叶子字段，label 左右布局，值超长换行；格式化渲染与卡片一致。
- pager 在已加载记录内切换正确；到达已加载末条且还有更多数据时自动请求下一页并进入新记录，失败可重试；到达首条时“上一条”按钮置灰；除该自动加载外不触发额外接口。

### 10.4 加载

- 首查、刷新、外部参数变更替换列表并回到第一页；触底与手动“加载更多”追加且不重复请求同一页。
- 旧响应不覆盖新查询；加载失败可重试且页码、已有卡片保留；`showPagination === false` 一次性加载。
- 预览态不请求字典；销毁后无异步回写。

### 10.5 兼容

- 桌面端渲染、保存、导入导出行为不受影响（仅新增顶层 `mobileAttrs` 键，`tableAttrs` 及其余结构不变）。

## 11. 与宿主项目的集成边界

lowcode 输出移动端列表渲染组件（`packages/mobileTable`）及其运行时 API、含顶层 `mobileAttrs` 的列表 JSON；宿主项目负责：

- 通过 `CommonDP utils` 的 `isMobile()`（UA 判定）得出运行环境，并在 `tableRender` 的 `provide` 中提供 `isMobile`，`complete-table` 通过 inject 读取；是否渲染移动端列表由 `complete-table` 内部控制，对外始终挂载 `complete-table` 一个组件（H5 入口恒为移动端环境，固定 provide `true`）。
- 传入与桌面端一致的列表 JSON、外部参数和动态参数。
- 提供既有 inject 能力面：`requestTableData`、`requestTablePaginationData`、`generalRequest`、`checkPermission`、`renderStrategy`、`getWidgetByFreeLayout`、`eventBus`、`getListPageId`、`getRawRelateId`、`enterpriseId` 等。
- 为列表容器提供确定高度（卡片列表内部滚动与详情页铺满父级的前提）。

宿主项目不得通过修改 JSON 为移动端增加隐式字段，也不得要求移动端自行猜测未定义的远端排序、远端筛选协议。

## 12. 第一期实现状态（2026-09-10）

### 12.1 已完成

以下条目均已实现并通过 webpack 全量构建验证（`lib/mobileTable.js|min.js` 产出；`completeTable` 为集成移动端新增 inject `isMobile` 切换与 API 路由，桌面端渲染路径与行为不变）：

| 契约条目 | 状态 | 说明 |
| --- | --- | --- |
| §2.1 设计端：顶层 `mobileAttrs` 四项配置 | ✅ | `baseConfig/tableBaseConfig.js` 新增 `getMobileAttrs()`；`mobileAttrsDlg.vue` 独立弹窗。 |
| §2.1 设计端：独立“移动端设置”入口 | ✅ | `operate.vue` 独立按钮 → `tableDesign.js` mixin → `table-widget` 打开/回写链路，与表格属性设置互不干扰。 |
| §2.1 设计端：保存、回显、导入导出兼容 | ✅ | `getRenderParams()` 输出顶层 `mobileAttrs`；加载按 `merge({}, getMobileAttrs(), 保存值 \|\| {})`；导入导出为全量结构自动包含。 |
| §3 输入配置契约（合并规则、回退规则、校验规则） | ✅ | 设计端与渲染端同规则；`mobileFields` 失效项设计端保存剔除、渲染端跳过并告警。 |
| §4 卡片渲染契约（结构、显隐、点击） | ✅ | 标题行不回退、单/双列先行后列（末尾奇数项占单格半行、不跨整行）、label 上下/左右、`align` 不消费（值固定居左）、超长截断、标题键不进字段区；点击打开详情 + `rowClick`（预览/禁用态不发布）。 |
| §5 详情页契约 | ✅ | 覆盖层铺满父级（自动补 `relative`）、返回不重查、label 固定左右、pager 已加载内切换 + 末条自动加载更多 + 失败停留可重试 + 首条提示、不请求详情接口。 |
| §6 字段渲染契约 | ✅ | 优先级 `formatter > contentTextAttrArr > PERSON > DICT > 原始值`；`textVal` 支持 `{}`/`{fieldName}` 占位符模板解析（与 web 端提交 `6475ec0` 一致）；字典 `dicCode` 运行时请求缓存、预览不请求、失败回退原值。 |
| §7 数据加载与并发契约 | ✅ | 分页/一次性、首查替换回第一页、触底与手动共用一法、`requestVersion` 拒旧响应、单飞、失败恢复页码、销毁取消观察禁止异步回写；查询参数与桌面 `getParams` 同构（`searchForm` 空默认值、日期拆分、数值转换、`advSearchExpr`/`advSearchExprJoinOp` 默认键、`page` 含 `totalCount`；搜索字段不区分桌面两种摆放）。 |
| §8 对外 API 与事件（第一期最小集） | ✅ | `init`/`expose_preview`/`expose_refreshData`/`expose_getTableData`/`expose_setTableData` + `rowClick`。 |
| §9 生命周期契约 | ✅ | `onInitEvent`/`onBeforeQueryDataEvent`（返回 `false` 取消）/`onAfterQueryDataEvent`，以移动端列表实例为 `this`，异常记录不破坏状态。 |
| §10.5 兼容 | ✅ | 桌面端仅新增顶层 `mobileAttrs` 键，`tableAttrs` 及其余结构不变。 |
| §11 集成边界 | ✅ | `complete-table` 经 inject `isMobile` 内部切换（宿主 `tableRender` provide，H5 入口固定 `true`），对外仅挂载 `complete-table`；查询参数与 web 端 `getParams` 同构。 |

### 12.2 验收口径调整记录

- **复杂 formatter 验收口径（§6.4）**：2026-09-10 拍板，口径以 §6.4 为准；真实环境逐个回归随宿主联调进行。
- **运行时联调（§10 运行时验收、§11 集成）**：不由 lowcode 渲染端负责，移交宿主侧联调（complete-table 内部切换集成、容器定高、真实列表 JSON 场景回归）。

### 12.3 遗留问题

| # | 问题 | 影响 | 状态 |
| --- | --- | --- | --- |
| 1 | **Element Table 专有 API 兼容代理**：历史 formatter 若调用 el-table 专有 API（`$refs.elTable`、`doLayout` 等），移动端无代理层，直接走"开发告警 + 回退该字段后续链路"兜底。 | 依赖 el-table 上下文的存量 formatter 在移动端显示为回退内容（内容文本/字典/人员/原始值），不影响整卡与整列表。 | 已决策（2026-09-11，方案a）：按 §6.4 既定行为执行（不提供兼容代理、“告警 + 回退”）；宿主联调期收集真实依赖清单，若高频依赖再评估针对性垫片 |
| 2 | **宿主运行时联调未做**：`§10.1–10.4` 的运行时验收、`§11` complete-table 内部切换集成与容器定高尚未在真实宿主环境执行。 | 交付质量依赖宿主联调确认；仓库内 `examples/` 无 inject 提供者，无法自测。 | 移交宿主侧（见 12.2） |
| 3 | 说明性偏差：`tree-table`/`tabs-table` 布局工具条可见“移动端设置”但事件未接线（一期仅覆盖 `pageLayout: "table"`）。 | 无功能影响，范围外。 | 无需处理（记录在案） |

