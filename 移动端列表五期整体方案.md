# lowcode 移动端列表五期整体方案

## 0. 文档定位

本方案是《移动端列表渲染方案.md》《移动端列表第一期实现契约.md》的细化与调整版。三份文档的关系：

- 本文档：五期划分的总方案（第一部分）、设计端与渲染端的具体事项（第二部分）、与既有方案的微调对照（第三部分）。
- 《移动端列表渲染方案.md》：保留作为各能力域的语义定义（字段渲染、按钮、搜索、外部 API 的规则仍有效），已按本方案微调分期结论。
- 《移动端列表第一期实现契约.md》：已按本文档重写为新一期实现契约，范围与本文档 1.9 节第一期一致、验收标准与 1.10 节一致；开发联调时两份配合使用。
- 2026-09-18 起：第五期的按钮体系部分口径已确认并拆分为第六/七/八期实施，逐类动作口径、两个未确认问题的结论、按钮入口投放与拆分详见本文档**第四部分**；原第五期的复杂布局部分维持暂不考虑。

本期需求输入（对应设计图）：

1. 设计端设置标题键。
2. 卡片字段可选一列或两列排布。
3. 设计端选择移动端展示列。
4. Label 展示可选上下（label 在上、值在下）或左右（label 在左、值在右）。
5. 操作列暂不考虑。
6. 格式化渲染展示（字典标签、人员头像、内容文本样式等）移动端同样需要。
7. 点击卡片后通过 JS 控制，详情页铺满当前父级空间；详情页 label 左右展示，格式化渲染同样需要。
8. 搜索区域先不考虑。
9. 筛选不再区分为表头筛选和列表筛选，统一放在列表最上方。
10. 排序入口同样放在列表最上方（顶部工具条交互，对应图 3–5）。
11. 标题行只有一行、仅渲染标题键；去掉状态字段配置，未配置标题键则标题行整体不展示（不回退其他字段）。
12. 详情页翻页需考虑分页：一次加载十行、当前正好是末行时，点击“下一条”应自动请求接口加载下一页。
13. 无论卡片还是详情页、标题还是其他字段，只要字段配置了格式化渲染就优先使用格式化渲染；无配置展示纯文本。
14. `mobileFields` 为空时默认取 `tableOptions` 中前 4 个 `show` 为真的字段；不足 4 个的有几个展示几个。（实现口径见 1.3：过滤失效项与标题键后无剩余有效列时同样回退。）

---

## 一、整体方案

### 1.1 目标与总原则

- 移动端渲染器消费与桌面端同一份列表 JSON，并新增**与 `tableAttrs` 平级的独立顶层配置键 `mobileAttrs`**（不放入 `tableAttrs`）。设计端加载与渲染端解析都按 `merge({}, 默认值, 保存值)` 合并，未来新增配置字段时旧页面自动补默认值、不失效；旧 JSON 无该键时按回退规则渲染，不做数据迁移，不影响桌面端。
- 移动端不复用桌面 `el-table` UI，渲染为**单列卡片列表 + 详情页**两层结构：卡片展示摘要字段，点击卡片在当前父级容器内打开详情页查看全部字段。
- 第一期不做：搜索区域（第四期）、操作列与按钮入口（第六~八期，原第五期按钮体系，2026-09-18 拆分）、选择/批量（第三期）。
- 复用优先：桌面端数据查询、参数组装、生命周期、外部 API 的语义与 `completeTable` 保持一致；与 `el-table` 解耦的逻辑（`getParams`、`queryTableData`、字典加载等）按同语义实现并在代码中标注来源。
- 预览态、禁用态不发布业务事件、不执行按钮。

### 1.2 设计输入（五张设计图的结构化结论）

**图 1 · 卡片列表页**

- 单列卡片列表，卡片之间留白，卡片整体圆角。
- 卡片结构分两层：
  - 标题行：单独一行，仅渲染标题键字段值（如日期文本）；未配置标题键则整个标题行不展示。
  - 字段区：按配置一列或两列网格（`fieldLayout` 仅支持 `single` / `double` 两种），每个字段 label 在上、值在下（即 `labelLayout: vertical` + `fieldLayout: double` 的组合）。
- 字段值的格式化渲染一律来自字段配置（字典胶囊、人员头像、内容文本样式等），无格式化配置的字段展示纯文本。
- 顶部导航栏、状态栏属于宿主页面，不在列表组件范围内。
- 右下角浮动“+”按钮属于按钮体系（第六期，原第五期拆分），第一期不渲染。

**图 2 · 详情页**

- 导航栏：返回按钮 + “详情”标题（详情页组件自带，因为它以覆盖层形式铺满父容器）。
- 大标题区：标题键字段值。
- 字段行列表：label 居左（灰色小字）、值居右，行与行之间细分隔线；值超长换行完整展示。
- 底部 pager：`1/31` + 左右箭头，在当前已加载记录内切换上一条/下一条。

**图 3–5 · 顶部筛选与排序工具条（第四期交互基准）**

- 列表最上方、卡片列表之上的常驻横向工具条：横排若干“`字段名` ▾”下拉入口（如 状态、发起人、日期）+ 排序入口，超出宽度横向滚动。
- 点击入口在工具条下方展开该字段的选项面板：选项列表单选（含“全部”清空），选项多时提供“展开更多/收起”；下拉、字典、级联、日期等按搜索控件类型渲染对应面板。
- 入口在存在选中值时高亮并显示所选摘要；排序入口选中后显示“字段名 ↑/↓”。

### 1.3 移动端配置协议 `mobileAttrs`

移动端配置是保存 JSON 中**与 `tableAttrs` 平级的独立顶层键 `mobileAttrs`**，不放入 `tableAttrs`：

```json
{
  "pageLayout": "table",
  "tableOptions": [],
  "formOptions": [],
  "tableAttrs": {},
  "mobileAttrs": {
    "titleField": "",         // 标题键，fieldCode；空则标题行不展示
    "mobileFields": [],       // 移动端展示列，fieldCode 有序数组，顺序即卡片展示顺序
    "fieldLayout": "double",  // 卡片内字段排布：single 一列 | double 两列（仅此两种）
    "labelLayout": "vertical" // label 排布：vertical 上下 | horizontal 左右
  },
  "fuzzyFieldSearchConfig": {},
  "keyField": ""
}
```

默认值与合并（前向兼容，设计端与渲染端同规则）：

- 默认值工厂：`getMobileAttrs()` 返回 `{ titleField: "", mobileFields: [], fieldLayout: "double", labelLayout: "vertical" }`，独立于 `getTableAttrs()`，不修改 `tableAttrs` 结构。
- 设计端加载（编辑态）：`this.mobileAttrs = merge({}, getMobileAttrs(), json.mobileAttrs || {})`。
- 渲染端解析（运行态）：同规则合并后再走回退判断；未知字段忽略。
- 目的：以后 `mobileAttrs` 新增配置字段时，旧页面（保存值里无该字段）自动取默认值，不失效、无需迁移。

字段说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `titleField` | string | 否 | 卡片标题行与详情页大标题使用的字段。从全部 `show` 叶子列中选择，不要求属于 `mobileFields`。已作为标题的字段不再重复出现在卡片字段区。未配置时标题行不展示，不回退到任何字段。 |
| `mobileFields` | string[] | 否 | 卡片摘要展示列。子集 + 排序均由设计端控制；为空或过滤失效项与标题键后无剩余时回退前 4 个 `show` 为真值的叶子列（剔除标题字段后；不足 4 个有几个展示几个）。 |
| `fieldLayout` | enum | 否 | `single` / `double`，默认 `double`。 |
| `labelLayout` | enum | 否 | `vertical` / `horizontal`，默认 `vertical`。 |

回退规则（旧 JSON 无 `mobileAttrs`、或字段为空时，渲染端按序回退，不阻断渲染）：

1. `titleField` 为空 → 标题行不展示（不回退到任何字段），字段区照常渲染。
2. `mobileFields` 为空，或过滤失效项与标题键后无剩余有效列 → `tableOptions` 中前 4 个 `show` 为真值的叶子列（已用作标题的剔除；不足 4 个时展示现有的全部）。仅影响卡片字段区，详情页始终展示全部显示字段。
3. `fieldLayout` / `labelLayout` 非法值 → 取默认 `double` / `vertical`。

校验规则（设计端保存时执行，渲染端容错）：

- `mobileFields`、`titleField` 必须是当前 `tableOptions` 中 `show` 为真值的叶子列的 `fieldCode`；渲染端遇失效项跳过并输出开发告警。
- `mobileFields` 为有序数组，顺序即卡片展示顺序（设计端勾选即按列顺序写入，不支持拖动排序，避免与桌面列顺序脱节）；渲染端按数组顺序渲染，不按 `tableOptions` 顺序重排。
- `fieldCode` 匹配数据时兼容返回数据字段名大小写差异（与 `keyField` 的 `resolveKeyFieldCase` 同语义，`tableItem.js:1404`）。

### 1.4 卡片列表渲染模型（第一期）

```text
┌──────────────────────────────────┐
│ 标题键字段值                      │  ← 标题行（未配置 titleField 则整行不展示）
├──────────────────────────────────┤
│ label一          label二          │  ← labelLayout: vertical（label 上、值下）
│ 值一             值二             │
│ label三          label四          │
│ 值三             值四             │
└──────────────────────────────────┘  ← fieldLayout: double 时两列网格；single 时单列
```

- 标题行仅渲染标题键字段值（走格式化渲染），未配置则整行不展示；标题与字段区的值一律优先使用字段配置的格式化渲染，无格式化配置展示纯文本。
- `labelLayout: horizontal` 时每个字段渲染为“label 左、值右”一行，与详情页字段行同构。
- 两列网格按 `mobileFields` 顺序先行后列排布（1,2 / 3,4 …）；末尾奇数项占单格半行、不跨整行。
- 卡片渲染列 = `mobileFields` ∩ `show` 为真值的叶子列；桌面 `show` 为假的列即使被配置过也剔除。
- 字段名（label）使用 `fieldName`；`renderHeader` 不执行（移动端无表头，明确降级）。
- 值超长单行截断（ellipsis），完整值在详情页查看；详情页字段值超长换行完整展示。
- `align`、`columnWidth`、`fixed`、`show-overflow-tooltip` 等桌面列属性不控制卡片布局。
- 含 `children` 的父表头列不参与卡片渲染（多级表头分组属第五期）。
- 卡片整体点击 → 打开详情页；同时更新 `currentSelectedRow` 并发布 `rowClick`（对齐桌面行点击语义）。字段点击行为第一期不响应；第二期已接入配置了 `clickEvent.relateBtnId` 的片段（优先于卡片点击并阻止冒泡，见 1.6 第二期补齐）。

**第三期补齐（已实现，2026-09-15）——卡片选择**：

- `tableAttrs.isShowCheckbox` 为真时每张卡片左侧渲染勾选框（Element UI 技术栈内自绘样式），勾选阻止冒泡、不触发卡片点击打开详情页；预览态/禁用态勾选框置灰不可交互。
- 单选/多选来源与桌面一致：`renderStrategy.singleSelect` 为真时单选（只保留最后选择项，改选即替换、取消即清空），否则多选；`renderStrategy.crossPageSelect` 控制跨页选择。
- 选择同一性优先按 `keyField` 判定（兼容已归一的大小写），`keyField` 缺失时退化为对象身份；勾选态、增删去重均按此口径，加载更多追加数据中的同主键记录显示已选且不重复添加。
- 替换式查询（首查/刷新/外部参数变更/`expose_setTableData`）收尾时：跨页选择开启且 `keyField` 有效 → 选择集合按 `keyField` 重锚（仍存在的记录保留并换为新对象，无主键值的记录不纳入保留集合并输出开发告警）；未开启跨页选择或 `keyField` 缺失 → 清空（对齐桌面 `reserve-selection=false` 数据替换即清空的行为）。加载更多追加数据时选择天然保留。
- 选择变化对外发布：父组件 `selectListHandler(selectList)` + FreeLayout eventBus `${widgetId}.selectionChange`（与桌面 `selectListHandler` 同链路）；程序性维护（重锚/清空）不发布选择事件，避免替换式查询级联触发业务事件。
- FreeLayout eventBus 四事件中 `rowClick`、`currentChange`（卡片点击且当前行实际变化时）、`selectionChange` 已生效；`rowDblclick` 移动端无双击交互入口，不绑定触发（语义见渲染方案 §7）。预览态/禁用态统一阻止业务事件发布。

### 1.5 详情页模型（第一期）

**呈现方式（JS 控制，非路由、非全屏 dialog）**：详情页组件以 `position: absolute; inset: 0` 覆盖在列表组件的父级容器内铺满（“沾满当前父级空间”）。约定：

- 列表组件挂载时检测父容器，未设置 `position: relative` 则自动补设，保证覆盖层定位正确。
- 宿主需为列表容器提供确定高度；详情页自身 `overflow-y: auto` 内部滚动。
- 关闭详情页（返回按钮）后列表回到原状态：数据、滚动位置不丢失，不重新请求。
- 列表数据被首查/刷新整体替换时，详情页当前下标越界即自动关闭；未越界时停留原下标。

**结构**：

```text
┌──────────────────────────────────┐
│ ←  详情                       │  ← 自带导航栏（返回 + 标题）
├──────────────────────────────────┤
│ 标题键字段值（大标题）             │
├──────────────────────────────────┤
│ label一 ······················ 值一│  ← 全部 show 叶子字段
│ label二 ······················ 值二│     label 左（灰）、值右
│ label三 ······················ 值三│     行间细分隔线，值超长换行
│ …                                │
├──────────────────────────────────┤
│           ←   1/31   →           │  ← pager：上一条/下一条
└──────────────────────────────────┘
```

规则：

- 字段范围：全部 `show` 为真值的叶子字段（含未进入卡片摘要的字段），固定 label 左右布局，不受 `mobileAttrs.labelLayout` 影响。后续如需详情页字段裁剪，扩展 `mobileAttrs.detailFields`（不在第一期）。
- 标题区：`titleField` 渲染结果（走格式化渲染）；未配置标题键则标题区不展示。
- 数据来源：直接使用当前行数据，**不重新请求详情接口**。
- pager：切换范围 = 当前已加载记录；到达首条时“上一条”按钮置灰；点击“下一条”到达已加载末条且 `finished === false` 时，自动触发加载更多请求（复用列表同一加载方法与并发约束：单飞、`requestVersion` 校验），成功后进入新加载的记录；加载中在 pager 上显示 loading；失败给出提示并可重试，停留当前记录。
- 预览态允许查看详情页（纯展示无风险），但不发布业务事件。

### 1.6 字段格式化渲染

渲染优先级与桌面一致（`formatter` 分派见 `BaseRenderTable/index.js:798-810`，其余见 `getCellRender` `:191-264`；桌面代码中另有 `slotName` 插槽、`tagName` 程序化组件渲染两层，因设计器保存的 JSON 不会产出这两个字段，移动端渲染链中省略）。完整优先级（各层互斥，命中即止）：

```text
formatter > contentTextAttrArr > PERSON > DICT > 原始值
```

各层含义与分期：

| 层级 | 是什么 | 配置来源 | 移动端分期 |
| --- | --- | --- | --- |
| `formatter` | 复杂自定义函数：`str2Fn` 执行返回组件定义（`components`/`data`/`computed`/`watch`/`methods`/`template`），`Vue.extend + Vue.compile` 渲染为组件 | 列“更多设置”代码编辑器 | 第一期 |
| `contentTextAttrArr` | 内容文本片段：条件判断 + 文本 + 样式 + 图标 + 点击关联按钮 | 列“点击行为”配置 | 第一期（点击第五期） |
| `PERSON` | `cellRenderType === "person"`：首字母头像 + 名称 | 列“更多设置”渲染类型 | 第一期 |
| `DICT` | `cellRenderType === "dict"`：字典映射 + `contentStyle` 彩色胶囊 | 列“更多设置”渲染类型 + 字典树 | 第一期 |
| 原始值 | `row[fieldCode]` 直接展示（纯文本） | — | 第一期 |

注意各层互斥：配置了 `formatter` 的列桌面端不再走内容文本/字典/人员渲染，移动端保持同样顺序。

格式化渲染完全由字段配置驱动：有 `contentTextAttrArr`、`cellRenderType`（PERSON/DICT）等配置才启用对应渲染，无任何格式化配置的字段展示纯文本（原始值）。该规则对卡片字段区、卡片标题行、详情页标题区与详情页字段行一致——凡有格式化配置的位置一律优先格式化渲染。

**第一期范围（卡片与详情页、标题与字段区均复用同一字段渲染器）**：

| 能力 | 处理 |
| --- | --- |
| 普通字段值 | 完整支持。 |
| 复杂自定义 `formatter` | 完整支持，执行上下文与兼容规则见下节，逐个验收。 |
| `cellRenderType === DICT` | `enumDisplayConfig.dicList` 匹配（`dicId === String(cellValue)`），`cnName` 回退原值；`contentStyle` 的 `backgroundColor`/`color` 渲染为圆角胶囊标签，无背景时纯文本。仅配 `dicCode` 时运行时请求 `/dic/content/list?dicCode=...` 并缓存，预览态不请求；失败回退原值并记录错误。 |
| `cellRenderType === PERSON` | 首字母头像 + 名称（`.user-tag` 语义）。 |
| `contentTextAttrArr` 样式 | 多片段按配置顺序渲染；`conditionalJudgment` 以列表实例为 `this` 执行、异常隐藏该片段；`textVal \|\| cellValue`，且 `textVal` 支持 `{}` 占位符（`{}` 取当前单元格值、`{fieldName}` 取行字段值，未匹配替换为空串，`{fieldName}` 按键名精确匹配行数据、不做大小写回退）；加粗/斜体/删除线/下划线/`color`/`textStyle`；`iconName`/`iconPosition`/`iconStyle`。 |
| `contentTextAttrArr` 点击 | 第一期不触发；第二期已接入：配置了 `clickEvent.relateBtnId` 的片段经共享 `executeButton` 触发并阻止冒泡（可执行动作子集见下方“第二期补齐”），未配置的片段样式照常渲染、不绑定点击。 |
| `renderHeader` | 不执行，label 使用 `fieldName`。 |

**复杂 `formatter` 上下文（第一期）**：

```js
renderMobileField({
  row, column, index,
  mode: "card" | "detail",
  mobileMode: true
})
```

- `str2Fn` 执行返回组件定义（`components`/`data`/`computed`/`watch`/`methods`/`template`），移植桌面 `Vue.extend + Vue.compile` 机制（`BaseRenderTable/index.js:745-841`）。
- `provide` 与桌面一致的能力：`getTableRenderInstance()`、`emitBtnClick(row, btnName, btnId)`；`emitBtnClick` 第一期为告警占位，第二期已完整生效（签名对齐桌面，见《移动端列表第一期实现契约》§13）。
- 不为依赖 Element Table 专有 API 的历史 formatter 提供兼容代理（2026-09-11 决策）：渲染机制与 web 端 `BaseRenderTable` 保持一致，此类脚本执行/编译异常时开发告警并回退该字段的后续渲染链路（内容文本/字典/人员/原始值），不得导致整卡/整列表渲染失败。
- 所有现有复杂 formatter 的验收口径：渲染机制与 web 端 `BaseRenderTable` 一致即视为满足，真实环境逐个回归随宿主联调进行，不作为交付阻塞项。

**第二期补齐（已实现，2026-09-14）**：

- 字段点击行为（`contentTextAttrArr.clickEvent.relateBtnId`）接入：配置了关联按钮的片段可点击，优先于卡片点击并阻止冒泡（与桌面 `clickBtn` 语义一致）；未配置关联按钮的片段不绑定点击、不影响卡片点击打开详情页。
- 按钮执行器共享抽取已落地：新增 `packages/completeTable/component/executeButton.js`（逐字移植 `tableItem.js` `handleBtnClick`/`autoValidate`/`validateSelectList`，`this` 改为宿主实例）；桌面 `tableItem.handleBtnClick` 改为薄壳调用，行为不变；移动端 `mobileTable` 作为宿主接入（全量构建验证通过）。
- 移动端 `emitBtnClick(row, btnName, btnId)` 完整生效（签名与桌面一致，`btnId` 优先、按钮名兜底；查找范围 = `formOptions` 权限过滤后的按钮列表，权限过滤与桌面 `filterBtnsByPermission` 同语义；预览态/禁用态不执行）。
- 移动端可执行的动作子集：自定义 `fn`、`openType=1` 当前页跳转（sessionStorage 传参，同桌面）、`openType=3` 新窗口、`openType=5` 接口请求（含 `isRefresh` 回到第一页重查）、`openType=-1` 的 `refresh`；其余动作（`0/2/4/6` 弹窗体系、下载/删除/导入/二维码等）输出开发告警，第五期接入。行按钮传入 `rowData` 时不受“无勾选”校验阻断（与桌面语义一致）。
- `dbClickRelateBtnId`：执行链已就绪（经 `emitBtnClick` 可达），但移动端无行双击交互（卡片点击打开详情页，不自动等同双击），不绑定触发入口；交互形态随第五期按钮体系确定。

### 1.7 数据加载与并发（第一期）

- 查询语义与桌面一致：参数合并顺序按 `getParams`（`tableItem.js:1313-1350`：`prjId` < 一次性参数 < searchForm < 范围转换 < `multiFieldSearch` < `enterpriseId` < `advSearchExpr` < `externalParams` < `dynamicExternalParams`）；searchForm 空默认值（数值范围 Start/End null 键、日期拆分、数值转换）与 `advSearchExpr`/`advSearchExprJoinOp` 默认值已同构实现（第一期无搜索 UI，值为空默认值，第三期 UI 接入后生效；移动端搜索字段不区分"列表上方/表头下方"，统一一处处理）；非项目路由删 `prjId`；`page` 形状含 `totalCount`。
- 分页：`showPagination === true` 走 `requestTablePaginationData`（`pageNo`/`paginationSize`）；`false` 走 `requestTableData` 一次性加载。第一期无搜索/筛选入口，仅首查、刷新、外部参数变更触发替换式查询。
- 生命周期：`onInitEvent`（配置解析后、首查前）、`onBeforeQueryDataEvent(params)`（同步返回 `false` 取消请求）、`onAfterQueryDataEvent(params, res.data)`（成功后），以移动端列表实例为 `this`；`onResetBtnEvent` 随搜索第四期接入；预览态不执行生命周期钩子、不发起查询，仅以本地样例数据渲染。
- sessionStorage 跳转参数：`init` 在 `onInitEvent` 后读取键 `lowcodeTableThisPageJumpParams`（与桌面端一致），为对象时合并进 `externalParams` 并随即删除该键；预览态不执行。
- 加载更多：触底（IntersectionObserver 哨兵）+ 手动“加载更多”按钮双通道，共用同一加载方法，递增页码追加数据；`totalCount` 判断是否还有更多。
- 并发治理（桌面端当前没有，移动端新增）：首查/刷新类请求使用递增 `requestVersion`，旧响应不得回写任何状态（含被抢占的加载更多不回写页码）；同一时间只允许一个加载更多请求（重复触底忽略）；加载更多失败恢复页码、保留已有卡片、允许重试；首查/刷新失败保留已有卡片、页码按已加载数据反推并提示；替换成功后当前行/选择集合按 `keyField` 重锚（仍存在则保留并换为新对象，否则移除；移动端展示全部已加载数据，不无条件清空）；组件销毁后禁止异步回写。
- `keyField` 兼容返回字段大小写差异（`resolveKeyFieldCase` 语义），为第三期跨页选择打底。

### 1.8 顶部筛选与排序区模型（第四期，已实现）

对应图 3–5。移动端不再区分桌面端的“表头筛选”与“搜索控件位置（above / below-header）”，筛选与排序统一收纳为**列表最上方的常驻横向工具条**。本节为第四期的交互与语义基准，已于 2026-09-18 实现（实现口径与决策见下方“第四期补齐”）。

```text
┌──────────────────────────────────────┐
│ [ 模糊搜索框（有配置才渲染）]  [ 搜索 ] │
├──────────────────────────────────────┤
│  排序 ▾                     全部筛选 ▾ │  ← 左右两按钮，分别打开下拉弹层
├──────────────────────────────────────┤
│ 默认排序 ✓                             │  ← 弹层自工具条下方顶部下探展开，
│ 字段一                                │     蒙层压暗下方列表区（工具条可点）
│ 字段二        升序 ✓                   │
│ （点选即生效并关闭；同字段再点           │
│   在升/降间切换，清空走「默认排序」）     │
└──────────────────────────────────────┘

全部筛选弹层（同样自顶部下探）
┌──────────────────────────────────────┐
│ 字段一                                │
│ [全部][选项一][选项二][选项三]          │  ← pill 按钮组，单选
│ 字段二                          展开 ∨ │
│ [全部][选项一][选项二] …               │
│ …（表单区超出弹层高度时内部滚动）        │
├──────────────────────────────────────┤
│ [   重置   ]   [   确认   ]     │  ← 等宽，固定不随滚动；确认制：确认统一生效
└──────────────────────────────────────┘
```

> 交互形态调整（2026-09-18，按最新设计反馈）：初版图 3–5 的“横排每字段下拉入口 + 工具条尾部重置”调整为上述“排序/全部筛选左右两按钮 + 下拉弹层（自工具条下方顶部下探）”模型；筛选从逐字段即时生效改为弹层草稿 + 确认统一生效；模糊搜索行右侧固定“搜索”按钮，点击后才调接口。

按钮规则：

- 模糊搜索框固定在工具条上方，右侧固定“搜索”按钮（点击后才调接口）：以当前关键字 `pageNo = 1` 重查；回车、清空输入等同点击“搜索”。
- 搜索框下方为左右两个文字按钮：靠左“排序”（选中排序后显示“字段名 ↑/↓”摘要并高亮）、靠右“全部筛选”（任一筛选条件生效时高亮）。无可排序字段或无有效筛选字段时对应按钮不渲染。
- 两按钮分别打开排序/筛选**下拉弹层**：弹层锚定列表区顶部（紧贴工具条下方）向下展开、上缘无圆角下缘圆角、最高约 2/3 容器高度内部滚动；蒙层压暗列表区（工具条在蒙层外保持可点，可切换另一弹层或再点收起）；同屏互斥。
- 有效筛选字段 = `isSearchWidget === true` 且 `searchWidget` 有效的搜索控件（含 `position: "below-header"`，移动端不保留表头位置）∪ 列 `filters`/`filtersConfig` 表头筛选；同一字段两种来源都有时合并为一个分组。

弹层规则：

- 排序弹层为选项列表：首行“默认排序”（清除排序条件），其后每个 `sort` 为真值的列一行；当前排序列显示“升序/降序”标注并勾选；**点选即生效并关闭**，同字段再点在升/降间切换（清空排序统一走“默认排序”行）。排序协议对齐桌面现状（原“优先配置的 `sort-method`，仅本地排序”口径随桌面提交 939194c 废弃 `sort-method` 同步调整）：桌面现行为全部 `sort: true` 列 `sortable: "custom"` 后端排序，移动端同口径——升/降提交 `externalParams.sqlConfig = { sort: [{ field, order: "asc" | "desc" }] }`、取消提交 `{ sort: [] }`，经 `refreshData` 等价的持久化语义合入 `externalParams`（翻页与后续查询自动携带）；移动端不做本地排序。
- 全部筛选弹层为分组表单：每个有效筛选字段一个分组（组标题 = 字段名，选项超过 8 个时组标题右侧提供“展开/收起”）；选项列表类（`filters`、下拉、字典）渲染为换行平铺的 pill 按钮组（单选 + “全部”清空）；文本/数值范围/日期/日期范围/级联按 `searchWidget` 类型渲染对应控件（保留 `tagAttrs`、远程选项、字典请求与 `autoFillOptions` 语义）；全部控件绑定草稿，底部“重置/确认”（等宽）。表单区超出弹层高度时内部滚动，底部按钮栏固定不随滚动。
- 数值范围不完整不提交（确认时按未填写处理）；日期范围拆 `${field}Start`/`${field}End`。

查询语义：

- 筛选为**确认制**：弹层内全部变更只写草稿，点击“确认”整体写回 `searchForm` 并 `pageNo = 1` 替换式重查、关闭弹层；点蒙层/再点按钮收起即放弃草稿。
- 排序点选即查询；“默认排序”清除条件后查询。
- 筛选弹层“重置”：`tableAttrs.onResetBtnEvent` 有配置时完全接管（关闭弹层，`str2Fn(...).call(this, cloneDeep)`，桌面 `handleFilterReset` 同语义）；否则仅清空草稿回空默认形状（弹层保持打开），确认时才生效。
- 桌面“本地模式”（vform 从表 `renderMode === 0` 的本地过滤/分页，依赖 `getWidget` inject）在移动端无对应场景（移动端未 inject `getWidget`），第四期不实现“本地筛选副本”更新路径。
- 列 `filters`/`filtersConfig` 选项与搜索控件统一分组，选中值经 `searchForm` 同通道提交（与搜索控件同形状的等值条件）；桌面端列筛选为 el-table 纯本地过滤、无远端协议，移动端不发明独立的远端列筛选参数。
- `listeners.change` 以列表实例为 `this` 保留原有回调，不被弹层交互吞掉（确认后对实际变化的字段逐个触发）。
- 模糊搜索框固定在工具条上方，右侧“搜索”按钮（语义见上方按钮规则）；回车等同点击“搜索”、清空输入等同当前关键字为空时点击“搜索”。
- 筛选、排序变更走替换式查询，选择集合按 1.4 第三期补齐的替换规则处理（跨页选择开启时按 `keyField` 重锚、未开启时清空）。

**第四期补齐（已实现，2026-09-18）——顶部筛选与排序（按最新设计反馈调整为“两按钮 + 顶部下探下拉弹层”模型）**：

- 工具条常驻组件顶部（不随卡片滚动，组件根布局调整为纵向 flex：工具条 + 列表区容器 `.mt-body`），内容 = 模糊搜索框（有配置才渲染，右侧固定“搜索”按钮）+「排序/全部筛选」左右两文字按钮（排序按钮选中后显示“字段名 ↑/↓”摘要；全部筛选按钮在任一筛选条件生效时高亮）；渲染条件 = 有模糊搜索配置、任一有效筛选字段或任一可排序字段。
- 下拉弹层：排序/筛选同屏互斥（`activeFilterKey`），锚定列表区顶部（紧贴工具条下方）向下展开、下缘圆角、最高约 2/3 容器高度，带蒙层淡入与弹层下滑动画；蒙层压暗列表区，工具条在蒙层外保持可点（切换另一弹层或再点收起）、点蒙层收起；层级低于详情页。
- 排序弹层：首行“默认排序”（清除排序）+ 每个可排序字段一行（当前排序行显示“升序/降序”并勾选）；点选即生效（提交 `sqlConfig`）并关闭，同字段再点在升/降间切换（清空排序统一走“默认排序”行）。
- 全部筛选弹层（确认制）：打开时从当前查询条件构建草稿（`filterDraft`），弹层内全部变更只写草稿；点击“确认”整体写回 `searchForm`、`pageNo = 1` 替换式重查并关闭；点蒙层收起即放弃草稿。每个有效筛选字段一个分组（组标题 = 字段名，选项超 8 个时组标题右侧“展开/收起”）；选项列表类（下拉/字典/列筛选来源）渲染为换行平铺 pill 按钮组（单选 + “全部”清空草稿）；文本/数值范围为输入控件、日期/日期范围为 `el-date-picker`（`tagAttrs` 透传）、级联为 `el-cascader`（`emitPath: false`，`expandTrigger` 移动端固定 `click`，无 hover）。表单区超出弹层高度时内部滚动，底部按钮栏固定不随滚动。multiple 控件仍单选 UI，但提交单元素数组保持桌面请求形状。
- 筛选弹层“重置”：`onResetBtnEvent` 有配置时完全接管（关闭弹层，桌面 `handleFilterReset` 同语义）；否则仅清空草稿回空默认形状（弹层保持打开供继续调整），确认时才生效。数值范围草稿不完整时确认按未填写处理（不提交不完整范围），不再阻断整体确认。
- 模糊搜索框仅 `fuzzyFieldSearchConfig.searchFieldList` 非空时渲染（顶层键本期接入解析，与桌面 `renderOperateArea` 同判定）；输入即 trim 绑定 `multiFieldSearch`，点击“搜索”/回车触发 `pageNo = 1` 重查。
- 有效筛选字段与桌面搜索区同口径不按 `show` 过滤；分组标题优先取 `searchWidgetConfig.formItemAttrs.label`（桌面搜索区标签同来源），未配置回退 `fieldName`；按 `sortNumb` 升序稳定排序（桌面 `formOptions` 同排序）。
- 远程选项（`autoFillOptions`、`request.require` + `url`）按 `fieldCode` 缓存一次、筛选弹层打开时按需加载，语义与桌面 `BaseRenderForm.disposeRequest` 逐字对齐（远程结果整体替换静态选项、字典 `labelTranslateType === 1` 的 `dicId-` 前缀与 `{key:"dicId",label:"cnName"}` 强制映射仅限 `require`+`url` 分支——autoFill 结果恒为 `id/cnName` 形状不强制、`sortNum` 排序、autoFill 请求体含查询参数）；预览态不请求。
- 列筛选选项优先级 `filters` > `filtersConfig.customHandler` > `isFilter`；`customHandler(tableData)` 与 `isFilter` 派生在弹层打开时对当前数据现算（等价桌面 `tableDataChangeQueue` 重算语义）；`isSplit`/`splitChar`/`limitShowWord`/`maxlength` 语义一致（截断仅影响显示文本，提交值保持原始值）。
- `listeners.change` 以列表实例为 `this`、查询先触发原回调后执行（与桌面 `addEventListener` 包装顺序一致）；确认均为显式触发（点选/确认/回车），无桌面 300ms 防抖需求（亦无防抖需在销毁时清理）。
- 排序、筛选确认、模糊搜索变更统一走 `loadFirst()` 替换式查询：`requestVersion` 并发治理与选择集合重锚/清空（1.4 第三期补齐规则）自动生效，无新增收尾代码。

### 1.9 分期划分（一至五期 + 六/七/八期拆分）

| 期次 | 主题 | 范围概要 | 状态 |
| --- | --- | --- | --- |
| 第一期 | 配置 + 卡片 + 详情页 | 设计端 `mobileAttrs` 四件套（标题键/展示列/单双列/label 布局）；单列卡片列表；点击卡片 JS 铺满父级的详情页（label 左右 + 上一条/下一条，末条自动加载下一页）；格式化渲染（普通/字典/人员/内容文本样式/复杂 `formatter`，配置驱动）；数据加载、触底/手动加载更多、并发治理 | **已实现（2026-09-10，遗留问题见《移动端列表第一期实现契约》§12.3）** |
| 第二期 | 字段交互 | 字段点击行为（`contentTextAttrArr.clickEvent`）+ 按钮执行器共享抽取 `executeButton`，使 `emitBtnClick` 完整生效 | **已实现（2026-09-14）**：共享 `executeButton` 抽取落地、`emitBtnClick` 完整生效、字段片段点击接入；`dbClickRelateBtnId` 执行链就绪但移动端无双击交互入口（见 1.6 第二期补齐） |
| 第三期 | 选择与集成对齐 | 卡片选择（`isShowCheckbox`）、单选/多选/跨页选择（`keyField` 去重恢复）；对外事件全集（`selectListHandler`、FreeLayout 四事件）；`expose_*` API 全集对齐、预览态/禁用态细化；设计器内移动端预览 | **已实现（2026-09-15）**：卡片勾选框（单选保留最后选择项、多选按 `keyField` 去重、跨页选择替换式查询后按 `keyField` 重锚/未开启时清空、无主键记录不保留并告警）；`selectListHandler` + FreeLayout `selectionChange`/`rowClick`/`currentChange`（`rowDblclick` 移动端无双击交互入口）；`expose_setSearchForm`/`expose_setTableDisbaled`/`expose_enableAllBtn`/`expose_CompleteTableInstance` 落地（`expose_showDialog`/`expose_hideDialog` 为第五期弹窗体系占位告警）；禁用态细化（`tableDisbaledStatus` 与 `dynamicExternalParams` 联动）；设计器预览弹窗支持桌面/移动端切换（手机壳内 provide `isMobile`） |
| 第四期 | 顶部筛选与排序 | 搜索框（右侧“搜索”按钮，点击才调接口）下方「排序/全部筛选」左右两按钮：排序打开下拉弹层选项列表（点选即生效，升/降互切，清空走“默认排序”），全部筛选打开下拉弹层分组表单（pill 按钮组 + 控件 + 重置/确认，表单区滚动、底部按钮固定，确认制草稿）；弹层自工具条下方顶部下探、蒙层压暗列表区；筛选字段为搜索控件（含 `below-header`）与表头 `filters`/`filtersConfig` 统一分组；排序对齐桌面 `sqlConfig` 后端协议（原“`sort-method` 本地排序”口径随桌面 939194c 废弃该配置同步调整，本地模式不适用移动端） | **已实现（2026-09-18）**：工具条 + 排序/筛选下拉弹层（蒙层）、模糊搜索（搜索按钮）、七类控件分组、列筛选经 `searchForm` 提交、`sqlConfig` 后端排序、`onResetBtnEvent` 接入（筛选弹层“重置”）；实现口径与决策见 1.8 第四期补齐 |
| 第五期 | 按钮与复杂布局 | 按钮体系部分于 2026-09-18 确认口径后拆分为第六/七/八期实施（逐类动作口径、入口投放、表单/列表路由页设计与拆分详见**第四部分**）；复杂布局部分（`tabs-table`、`tree-table`、多级表头分组、汇总、二维码、打印导出、导入、插槽/关联自定义组件、`isMerge`/`spanMethod`）维持暂不考虑 | 按钮体系已拆分为六/七/八期（待实施）；复杂布局暂不考虑 |
| 第六期 | 按钮入口与体系骨架 | 四处按钮入口 UI（右下角 FAB（`add`）/ 卡片操作区（行级）/ 勾选态底部操作栏（批量）/ 页面顶部（其余全局，与已实现的第四期工具条统筹））；按钮可见性与场景过滤、预览/禁用置灰；`dbClickRelateBtnId` 定案不提供触发入口、`openType=4` 关联组件定性暂不实施、`expose_showDialog`/`expose_hideDialog` 维持占位告警；已实现动作（自定义 `fn`/`openType` `1`/`3`/`5`/`-1` `refresh`）回归矩阵 | **已实现（2026-09-18）**：投放推导（`btnPlacement` 四组分组）+ 四处入口 UI（顶部第二行按钮行、右下角 FAB（单触发/多聚合半屏面板）、卡片操作区（常用 ≤3 直出 + “···”半屏面板）、勾选态底部批量栏骨架）、可见性与场景过滤（`isHidden` 不渲染、FreeLayout `btnVerifyType` 分支、预览/禁用置灰）、与第四期弹层同屏互斥、回归矩阵自测 30/30 通过；实现口径与决策见 4.9 |
| 第七期 | 流程与批量动作 | `openType=2` 流程跳转流程 H5 摘要页（check 查流程实例、发起/审批查流程定义、`approveType` 映射确认）；`isRefresh` 返回回刷机制；批量操作闭环（底部操作栏联动 `validateSelectList`、`batchDel` 与桌面同语义补实现、`deliverySelectList` 传参） | 待实施（`tableRender.vue` provide 已含 `queryFlowDef`/`requestBatchDel`，移动端补 inject 声明即可） |
| 第八期 | 表单/列表专用路由页 | `openType=0/6` 经宿主新建路由 + 仿 `commonRender/index.vue` 公共入口分流渲染：`renderType=pageList|form`（pageList → `tableRender.vue` 内部 provide `isMobile` 自动切 `mobileTable`、form → `vFRender_view.vue` 自带 H5 适配）；底部“确定/取消”调接口链路（`submitForm()` → `/dyn-common/save|update/${formId}`）、编辑回填与只读、`externalParams` 注入、返回重查 | 待实施（**渲染入口已确认、gate 解除（2026-09-18）**：表单 `vFRender_view.vue`、列表 `tableRender.vue`，见 4.3/4.4） |

第一期任务清单：

设计端（详见 2.1）：`mobileAttrs` 默认值、选项枚举、“移动端设置”弹窗（标题键/展示列/单双列/label 布局）、工具条入口与回写链路、导入导出兼容验证。

渲染端（详见 2.2）：新包 `packages/mobileTable`；配置解析与回退；卡片列表与卡片组件；字段渲染器（普通/字典/人员/内容文本样式/复杂 `formatter`，配置驱动）；字典请求与缓存；详情页（铺满父级、label 左右、pager 及末条自动加载）；触底/手动加载与 `requestVersion`；最小 `expose` 集（`init`/`expose_preview`/`expose_refreshData`/`expose_getTableData`/`expose_setTableData`）；`rowClick` 发布；空态/加载态/错误态。

### 1.10 第一期验收标准

**配置**

- 新设计的列表可配置标题键、展示列、单双列、label 布局；保存后 JSON 中顶层 `mobileAttrs` 正确且独立于 `tableAttrs`；重新打开设计器回显正确（加载时与默认值 merge）。
- 旧 JSON（无 `mobileAttrs`）打开设计器不报错，移动端按 1.3 回退规则渲染出完整卡片。
- `show` 为假的列不出现在移动端任何位置；`mobileFields` 含失效 fieldCode 时保存自动剔除、渲染跳过。

**卡片**

- 卡片为单列列表；标题行 = 标题键单独一行，未配置标题键时标题行不展示；字段区按 `fieldLayout` 单/双列、`labelLayout` 上下/左右正确排布。
- 两列网格顺序为先行后列；标题键字段不重复出现在字段区。
- 有格式化配置的字段（含标题键）按格式化渲染，无配置的字段展示纯文本；字典胶囊、人员头像、内容文本样式（含条件片段、图标）渲染正确；值超长截断。
- 复杂 `formatter`（返回组件定义）在卡片与详情页均可渲染；依赖 Element Table 专有 API 的历史脚本回退后续链路并输出开发告警；执行失败只影响当前字段。
- 点击卡片打开详情页，同时父组件收到 `rowClick(row)`。

**详情页**

- 以 JS 覆盖层铺满父级容器（父容器无 `position: relative` 时自动补设），非路由跳转；返回后列表数据与滚动位置不变。
- 展示全部 `show` 叶子字段，label 左右布局，值超长换行；格式化渲染与卡片一致。
- pager 在已加载记录内切换正确；到达已加载末条且还有更多数据时自动请求下一页并进入新记录（loading / 失败重试正确）；到达首条时“上一条”按钮置灰；除该自动加载外不触发额外接口。

**加载**

- 首查、刷新、外部参数变更替换列表并回到第一页；触底与手动“加载更多”追加且不重复请求同一页。
- 旧响应不覆盖新查询；加载失败可重试且页码、已有卡片保留；`showPagination === false` 一次性加载。
- 预览态不请求字典；销毁后无异步回写。

**兼容**

- 桌面端渲染、保存、导入导出行为不受影响（仅新增顶层 `mobileAttrs` 键，`tableAttrs` 及其余结构不变）。

---

## 二、设计端与渲染端的具体事项

### 2.1 设计端（setupTable）事项清单

| # | 事项 | 文件 | 要点 |
| --- | --- | --- | --- |
| D1 | 新增 `getMobileAttrs()` 默认值工厂 | `baseConfig/tableBaseConfig.js`（在 `getTableAttrs()` L5-51 旁新增独立函数） | 默认 `{ titleField: "", mobileFields: [], fieldLayout: "double", labelLayout: "vertical" }`；独立于 `getTableAttrs()`，不修改 `tableAttrs` 结构。 |
| D2 | 设计端加载与合并 | `packages/setupTable/components/table-widget/index.vue`（`init()` L179-224 内，仿 `this.tableAttrs = merge({}, this.tableAttrs, tableAttrs)` L195 的既有模式） | 编辑态解构 `mobileAttrs` 并执行 `this.mobileAttrs = merge({}, getMobileAttrs(), mobileAttrs || {})`；旧 JSON 无该键自动补默认值，未来新增配置字段同样自动补，旧页面不失效。 |
| D3 | 选项枚举 | `baseConfig/tableSelectConfigs.js` | 新增 `fieldLayout`（`single`/`double`）与 `labelLayout`（`vertical`/`horizontal`）枚举，供弹窗下拉/单选使用。 |
| D4 | 新增“移动端设置”独立弹窗 | `packages/setupTable/components/dialogs/mobileAttrsDlg.vue`（新建，独立编写，不复用 `tableAttrsDlg.vue`） | 表单项：标题键下拉（全部 `show` 叶子列，可清空，清空即标题行不展示）、展示列复选列表（同上，参考 `setSearchFieldDlg.vue` 配置 `searchFieldList` 的交互模式）、单双列 radio（仅一列/两列）、label 布局 radio。提交时按 1.3 校验规则过滤失效项并回写。 |
| D5 | 工具条独立入口 | `packages/setupTable/components/operate.vue` | 与“表格属性设置”（L7）并列增加**独立的**“移动端设置”按钮，emit 到 mixin；与表格属性设置互不干扰。 |
| D6 | 打开/回写链路 | `mixins/tableDesign.js`（仿 `showTableAttrs`:149-151）+ `packages/setupTable/components/table-widget/index.vue`（仿 `showTableAttrsDlg`:266-272） | mixin `showMobileAttrs()` → table-widget `showMobileAttrsDlg()` → 弹窗确认后 emit `changeMobileAttrs` 回写 `this.mobileAttrs`（独立于 `changeTableAttrs` 链路）。 |
| D7 | 保存链路 | `table-widget/index.vue:534-546` `getRenderParams`、`layout/defaultTable.vue:59-111` | `getRenderParams()` **新增一行** `mobileAttrs: this.mobileAttrs`（不再借道 `tableAttrs` 透传）；布局层 `handleSubmitTableConfig` 与 `saveListConfigJSON` 对新增顶层键透传，无需其他改动；`pageLayout` 仍由布局层补充。 |
| D8 | 导入导出兼容确认 | `mixins/tableDesign.js:35-124` | 导入导出为 `getRenderParams` 全量结构，`mobileAttrs` 自动包含；导入时经 `table-widget.init()` 解构并按 D2 规则 merge；验证一次往返即可。 |
| D9 | 预览说明 | `components/dialogs/previewDlg.vue` | 第三期已提供设计器内移动端预览：operate.vue 新增“移动端预览”入口，预览弹窗内支持桌面端/移动端切换；移动端预览以 375×650 手机壳渲染，内层 provider 固定 provide `isMobile: true`（provide 无响应性，经 v-if 切换重建组件树保证注入正确），复用同一份 `renderParams` 与 `expose_preview` 样例数据渲染。 |

### 2.2 渲染端（新建 `packages/mobileTable`）事项清单

包结构对标 `completeTable`，第一期仅支持 `pageLayout: "table"`，其余布局渲染“移动端暂不支持”占位。

| # | 事项 | 说明 |
| --- | --- | --- |
| R1 | 包骨架与入口 | `packages/mobileTable/index.js`：`init(isPreview, json, options, externalTriggerQueryTableData, tableDisbaled)`（对齐 `completeTable/index.js:179` 签名），解析 JSON（`mobileAttrs` 按 1.3 规则与 `getMobileAttrs()` 默认值 merge，前向兼容）、识别 `pageLayout`、暴露 `expose_*`。非 `table` 布局渲染“移动端暂不支持”占位、不执行生命周期、不发起列表数据请求（注：配置解析先于布局判断执行，字段含 `dicCode` 时仍会发起字典预取，属实现现状）。构建与导出方式参照 `completeTable/main.js`。 |
| R2 | 复用策略 | 第一期对 `completeTable` 的改动仅限移动端集成（inject `isMobile` 切换渲染与对外 API 路由，桌面端渲染路径与行为不变）：`getParams` 参数组装（`tableItem.js:1313-1350`）、查询与生命周期（`:1358-1419`、`:827-829`）、字典加载 `getDicList`（`:980-989`）、`resolveKeyFieldCase`（`:1404`）按同语义在移动端实现，代码注释标注来源；第二期已抽 `executeButton` 共享（`packages/completeTable/component/executeButton.js`，桌面 `handleBtnClick` 改薄壳、行为不变），第三期评估数据层 mixin 统一。 |
| R3 | 卡片列表组件 | `MobileTable.vue`：根元素 `height: 100%; overflow-y: auto`（宿主容器需确定高度）；渲染卡片列表、加载态/空态/错误态、触底哨兵与“加载更多”按钮、`requestVersion` 状态机。 |
| R4 | 卡片组件 | `MobileCard.vue`：标题行（仅 titleField，未配置不渲染）+ 字段区网格（CSS Grid，`fieldLayout` 决定列数、`labelLayout` 决定字段内部结构）；卡片点击 → 打开详情页 + 更新 `currentSelectedRow` + `$emit("rowClick", row)`。 |
| R5 | 字段渲染器 | `MobileField.vue`：实现 1.6 第一期优先级链：普通值 / DICT（含胶囊样式，对齐 `BaseRenderTable/index.js:237-260`）/ PERSON（对齐 `:230-236`）/ `contentTextAttrArr`（条件片段、样式、图标，对齐 `:197-229`；片段点击已随第二期接入，配置了关联按钮才绑定，见 1.6 第二期补齐）/ 复杂 `formatter`（移植 `:745-841` 的 `Vue.extend + Vue.compile`，`provide getTableRenderInstance/emitBtnClick`，不提供兼容代理、异常告警回退）。卡片与详情页复用。 |
| R6 | 字典能力 | 仅配 `dicCode` 的列请求 `/dic/content/list?dicCode=...`，按 `dicCode` 缓存复用；预览态不请求；失败回退原值。 |
| R7 | 详情页组件 | 详情页以 `position: absolute; inset: 0` 覆盖父容器、自带导航栏（返回）、大标题区、label 左右字段行、底部 pager（1/N + 上一条/下一条，到达已加载末条且未 finished 时自动触发加载更多，成功后进入新记录）；按 1.5 规则实现。列表组件挂载时检测并补设父容器 `position: relative`（与 1.5/实现对齐）。注：第一期实现暂将 R3–R5 的卡片列表、卡片、字段渲染器与详情页合并在 `packages/mobileTable/index.js` 单文件内，属实现暂态，后续按 R3–R5 拆分为独立组件文件。 |
| R8 | 加载与并发 | 触底（IntersectionObserver）+ 手动按钮共用加载方法；单飞 `loadingMore`；`requestVersion` 拒绝旧响应；失败恢复页码；`beforeDestroy` 取消哨兵观察与未完成回写。 |
| R9 | 对外 API（第一期最小集） | `init`、`expose_preview`、`expose_refreshData(data, dynamicData)`、`expose_getTableData`、`expose_setTableData`；命名与参数语义对齐 `tableItem.js:675-741`。全集已于第三期对齐：`expose_setSearchForm(data, multiFieldSearch)`（重置空默认值→写入→第一页重查，`multiFieldSearch` 进入 `getParams`）、`expose_setTableDisbaled(bool)`、`expose_enableAllBtn()`、`expose_CompleteTableInstance()`（`expose_MobileTableInstance` 为既有别名）；`expose_showDialog()`/`expose_hideDialog()` 依赖第五期弹窗体系，现阶段为占位告警。 |
| R10 | 事件（第一期最小集） | 父组件 `rowClick`（卡片点击时发布）；第三期已对齐全集：`selectListHandler`（勾选变化时发布，`complete-table` 向宿主转发）与 FreeLayout eventBus 四事件中的 `selectionChange`/`rowClick`/`currentChange`（`rowDblclick` 移动端无双击交互入口，不绑定触发，见 1.6 第二期补齐说明）；预览态/禁用态不发布业务事件；程序性维护选择集合（重锚/清空）不发布选择事件。 |
| R11 | 宿主集成 | 复用与桌面相同的 inject 能力面：`requestTableData`/`requestTablePaginationData`/`generalRequest`/`checkPermission`/`renderStrategy`/`getWidgetByFreeLayout`/`eventBus`/`getListPageId`/`getRawRelateId`/`enterpriseId` 等（`tableItem.js:380-592` 清单）。宿主通过 `CommonDP utils` 的 `isMobile()`（UA 判定）在 `tableRender` 的 `provide` 中提供 `isMobile`，对外始终挂载 `complete-table`，移动端/桌面端由该组件经 inject 读取后内部切换（H5 入口固定 provide `true`）。 |
| R12 | 样式规范 | 不引入 vant 等新 UI 库（保持 Element UI 2 技术栈，仅用 message 等基础能力）；卡片/详情页自绘。视觉对齐设计图：卡片圆角、留白、标题/正文字号层级、胶囊标签、头像标签；基于 flex/grid 弹性布局适配容器宽度。间距/圆角/主色当前为固定值实现（卡片圆角 8px、卡片间距 10px、主色 `#409eff`），暂未暴露 CSS 变量，宿主定制需求按需再评估。 |
| R13 | `keyField` 兼容 | 引入 `resolveKeyFieldCase` 同语义处理，为第三期跨页选择打底；缺失时输出开发告警。 |
| R14 | 顶部筛选/排序（第四期，已实现） | 实现为 `packages/mobileTable/index.js` 内 `renderFilterBar` + `renderActiveSheet` 渲染方法组与“顶部筛选与排序（第四期）”方法组（延续 R7 单文件实现暂态，暂未拆独立 `MobileFilterBar.vue`）：模糊搜索框（右侧“搜索”按钮，点击才调接口）+「排序/全部筛选」左右两按钮，分别打开排序/筛选**下拉弹层**（自列表区顶部下探、蒙层压暗列表区、工具条保持可点、同屏互斥、点蒙层收起）；排序弹层选项列表点选即生效（默认排序清除、同字段再点升/降互切）；筛选弹层为字段分组表单（组标题=字段名、pill 按钮组单选+全部、文本/数值范围/日期/日期范围/级联控件、组内超 8 项“展开/收起”；表单区超出弹层高度内部滚动、底部按钮栏固定），确认制草稿 + 底部“重置/确认”（等宽，`onResetBtnEvent` 接管点），确认整体写回 `searchForm` 后 `loadFirst()` 替换式查询；`listeners.change` 保留；排序走 `sqlConfig` 后端协议（`externalParams` 持久化）。本地模式不实现（移动端无 vform 从表场景）。设计端无需新增配置，复用 `isSearchWidget`/`filters`/`sort` 既有配置；顶层 `fuzzyFieldSearchConfig` 解析本期接入（与默认值 merge 容错）。详见 1.8。 |

### 2.3 实施顺序与里程碑

| 里程碑 | 内容 | 依赖 | 状态 |
| --- | --- | --- | --- |
| M1 | 设计端 D1–D6：`mobileAttrs` 配置、加载合并与保存回显 | 无 | ✅ 完成 |
| M2 | 渲染端 R1–R2：包骨架、配置解析与回退、查询与生命周期 | M1 的 JSON（可先用手工 JSON 并行） | ✅ 完成 |
| M3 | R3–R5：卡片列表、卡片、字段渲染器（普通/字典/人员/内容文本/复杂 formatter） | M2 | ✅ 完成 |
| M4 | R7：详情页与 pager | M3 | ✅ 完成 |
| M5 | R6、R8：字典请求缓存、加载更多与并发治理 | M2 | ✅ 完成 |
| M6 | R9–R13 + 1.9 验收：expose/事件/样式变量/宿主联调 | M1–M5 | 部分：expose/事件/`keyField` 兼容已完成；宿主联调移交宿主侧（见契约 §12.2），Element Table 兼容代理已决策不做（"告警 + 回退"为既定行为，契约 §12.3 问题 1） |
| M7 | 第二期：`executeButton` 共享抽取、移动端 `emitBtnClick` 完整生效、字段片段点击（阻止冒泡） | M1–M6 | ✅ 完成（2026-09-14，全量构建验证；`dbClickRelateBtnId` 无双击交互入口，见 1.6 第二期补齐说明） |
| M8 | 第三期：卡片选择（单选/多选/跨页重锚）、`selectListHandler` 与 FreeLayout 事件、`expose_*` 全集与禁用态细化、设计器内移动端预览 | M1–M7 | ✅ 完成（2026-09-15，全量构建验证；选择语义细化见 1.4 第三期补齐，`expose_showDialog`/`expose_hideDialog` 为第五期占位告警） |
| M9 | 第四期：顶部筛选与排序（模糊搜索（搜索按钮）、排序/全部筛选下拉弹层、筛选分组表单（表单区滚动、底部按钮固定）、`sqlConfig` 后端排序、`onResetBtnEvent` 接入） | M1–M8 | ✅ 完成（2026-09-18，全量构建验证；交互形态按最新设计反馈多轮调整为“两按钮 + 顶部下探弹层 + 确认制”，实现口径与决策见 1.8 第四期补齐、R14） |
| M10 | 第六期：按钮入口与体系骨架（四处入口、可见性与场景过滤、预览/禁用置灰、`dbClickRelateBtnId`/`openType=4`/`expose_showDialog` 定性收口、已实现动作回归矩阵） | M1–M9 | ✅ 完成（2026-09-18，全量构建验证 + 冒烟自测 30/30；实现口径与决策见 4.9 第六期实现记录） |
| M11 | 第七期：流程跳转（`flowH5Summary`）与批量操作闭环、`isRefresh` 返回回刷机制 | M10 | 待实施（`tableRender.vue` provide 已含 `queryFlowDef`/`requestBatchDel`，移动端补 inject 声明） |
| M12 | 第八期：表单/列表专用路由页（宿主路由 + 仿 `commonRender/index.vue` 公共入口、`renderType` 分流、确定调接口、返回重查） | M11 | 待实施（gate 已解除：表单入口 `vFRender_view.vue` H5 适配已有、列表入口 `tableRender.vue` 自动切换，见 4.3/4.4） |

### 2.4 风险与开放问题

- **历史 formatter 兼容**（第一期最大风险）：处理与验收口径见 1.6（渲染机制与 web 端对齐、不提供兼容代理、“告警 + 回退该字段后续链路”兜底）；宿主联调期收集真实依赖清单，若高频依赖再评估针对性垫片。
- **宿主容器高度**：详情页铺满父级与列表内部滚动都依赖容器有确定高度；需在宿主集成文档中明确约定。
- **字典请求频率**：卡片滚动加载大量数据时字典按 `dicCode` 缓存复用，避免重复请求。
- **`mobileAttrs` merge 语义**：设计端与渲染端两侧 merge 均需产生新对象（深拷贝），避免设计器内多个列表实例或设计/渲染两侧共享引用。
- **开放问题**：详情页字段裁剪配置（`detailFields`）、卡片字段拖动排序、移动端会话级列设置，均不在第一至三期，按需再评估。

---

## 三、与既有方案的微调对照

### 3.1 调整项

| # | 旧方案（渲染方案 / 契约） | 调整后 | 备注 |
| --- | --- | --- | --- |
| 1 | 不修改设计器、不增加移动端专用配置（渲染方案 §1/§11、契约 §3/§13） | 新增与 `tableAttrs` 平级的顶层 `mobileAttrs`（标题键/展示列/单双列/label 布局），设计/渲染双侧按默认值 merge 前向兼容 | 本轮需求 1–4 的直接来源；不迁移旧配置、不影响桌面端 |
| 2 | 摘要字段由列顺序推导（单行/三行模式，渲染方案 §3.2） | 卡片字段由 `mobileAttrs` 显式配置决定 | 旧 JSON 走回退规则 |
| 3 | 卡片内“更多”展开/收起完整字段（渲染方案 §2/§3.3） | 点击卡片打开详情页：JS 铺满父级、label 左右、上一条/下一条 pager | 本轮需求 7；展开不重新请求详情接口的原则保留 |
| 4 | 搜索、表头筛选、本地排序属第一期必做（渲染方案 §5、契约 §6） | 整体移至第四期，第一期不渲染搜索区域 | 本轮需求 8 |
| 5 | 按钮（新增/编辑/删除/批量/字段点击等）属第一期必做（渲染方案 §4、契约 §7） | 按钮体系整体移至第五期，第一期不渲染任何按钮入口（含浮动”+”按钮）；`emitBtnClick` 所需执行器已于第二期抽取共享并使字段点击（`contentTextAttrArr.clickEvent`）生效，按钮 UI 入口仍待第五期 | 本轮需求 5 |
| 6 | 选择（单选/多选/跨页/批量）属第一期（渲染方案 §7、契约 §8） | 移至第三期；第一期仅维护 `currentSelectedRow` 并发布 `rowClick` | — |
| 7 | 复杂 `formatter` 属第一期必做（渲染方案 §6.2、契约 §5.3） | 维持第一期完整支持；`renderMobileField` 的 `mode` 取值调整为 `"card" / "detail"`；`slotName`、`tagName` 因设计器 JSON 不产出，从移动端渲染链中省略 | 本轮需求 6 及后续调整 |
| 8 | 三期划分（渲染方案 §10） | 五期划分（本文档 1.8） | 一期最好做，四期五期暂不考虑 |
| 9 | （无） | 新增详情页模型（1.5）；标题行仅渲染标题键、未配置不展示、不回退；详情页 pager 到达已加载末条自动请求下一页；格式化渲染配置驱动、无配置展示纯文本（标题与字段、卡片与详情页一致） | 新增内容 |
| 10 | 筛选承载为“筛选抽屉”，表头下搜索与列筛选分别处理（渲染方案 §5） | 筛选不再区分表头/列表，与排序一起统一为列表最上方的常驻筛选/排序工具条（本文档 1.8，对应图 3–5） | 本轮新增调整 |
| 11 | 排序“优先配置的 `sort-method`，仅本地排序，不发明远端参数”（1.8 初版、渲染方案 §5.3/§11.7） | 排序对齐桌面现状（提交 939194c 废弃 `sort-method` 后全部可排序列 `sortable: "custom"` 后端排序）：升/降 → `externalParams.sqlConfig = { sort: [{ field, order }] }`、取消 → `{ sort: [] }`，与桌面 `refreshData` 持久化语义一致 | 第四期实施时同步修订（2026-09-18） |
| 12 | （无） | 筛选弹层内置“重置”按钮（设计图 3–5 未含独立重置入口）：`onResetBtnEvent` 有配置时完全接管（关闭弹层），否则仅清空草稿、确认时生效 | 兑现契约“`onResetBtnEvent` 第四期随搜索接入”承诺 |
| 13 | 列筛选为桌面 el-table 本地过滤（`filter-method`） | 移动端与搜索控件统一分组，选中值经 `searchForm` 同通道提交（等值条件），不发明独立的远端列筛选协议 | 1.8“统一收纳”的具体化；桌面本地模式（vform 从表）移动端无对应场景 |
| 14 | 工具条为“横排每字段下拉入口 + 尾部重置”（图 3–5 初版） | 搜索框右侧固定“搜索”按钮（点击才调接口）；搜索框下方「排序/全部筛选」左右两按钮，分别打开下拉弹层（自列表区顶部下探，蒙层压暗列表区、工具条可点）；筛选从逐字段即时生效改为弹层草稿 + 确认统一生效；排序同字段再点升/降互切、清空走“默认排序”行 | 2026-09-18 按最新设计反馈调整 |

### 3.2 保持不变的关键决策

- 移动端与桌面消费同一份列表 JSON，桌面端行为零改动；`mobileAttrs` 只追加顶层键、不侵入既有结构，双侧 merge 保证前向兼容。
- `show` 使用真值语义（`Boolean(item.show) === true`），与桌面 `filter(item => item.show)` 一致。
- 字段渲染优先级 `formatter > contentTextAttrArr > PERSON > DICT > 原始值`（`slotName`、`tagName` 设计器 JSON 不产出，链中省略）。
- `renderHeader` 明确降级为 `fieldName`；`columnWidth`/`fixed`/`show-overflow-tooltip` 不控制移动端布局。
- 字典加载、匹配、`contentStyle` 颜色、失败回退规则不变。
- 查询参数合并顺序、生命周期时序（`onInitEvent` → `onBeforeQueryDataEvent` → 请求 → `onAfterQueryDataEvent`）与 `this` 语义不变。
- 外部 API 名称与参数语义（`init`/`expose_*`）不变，仅分期交付。
- 并发治理要求（`requestVersion`、加载单飞、失败恢复、销毁防回写）不变。
- 树表、页签表、多级表头、汇总、二维码、打印导出、导入、插槽：维持暂不实施。远端排序采用桌面既有 `sqlConfig` 协议（第四期已实现，见 1.8）；不发明独立的远端列筛选协议（列筛选选项经 `searchForm` 通道提交）。

---

## 四、第五期按钮体系口径确认与六/七/八期拆分（2026-09-18）

第四期（顶部筛选与排序）实现后，剩余未实施的就是原第五期。本部分逐类确认按钮动作的处理口径、回答两个未确认问题（表单“确定”调接口、组件支持现状）、给出按钮入口投放结论，并将按钮体系拆分为第六/七/八期实施；原第五期的复杂布局部分维持暂不考虑。按钮执行器（共享 `executeButton`）第二期已抽取落地，六/七/八期补的是**各动作的移动端承载与按钮 UI 入口**，不重复实现校验链。

### 4.1 结论摘要

- 按钮动作口径：流程按钮（`openType=2`）跳转宿主流程 H5 摘要页 `#/flowH5Summary`；动态表单/动态列表（`openType=0/6`）经宿主新建的一个路由 + 仿 `commonRender/index.vue` 的公共入口按 `renderType=pageList|form` 分流渲染；接口请求（`openType=5`）、当前页跳转（`openType=1`）、新窗口（`openType=3`）与桌面一致（第二期已实现）；批量删除（`openType=-1` `batchDel`）属“调用接口”类、与桌面同语义补实现；关联组件（`openType=4`）与下载/导入/二维码族维持暂不实施。
- 表单/列表渲染入口已确认（2026-09-18，gate 解除）：表单 = 宿主 `mainMg/lowcode/component/vFRender_view.vue`（自带 H5 适配，`submitForm()` 即“确定调接口”：校验 + `/dyn-common/save|update/${formId}`）；列表 = 宿主 `commonRender/components/tableRender.vue`（provide `isMobile` 自动切移动端卡片列表）。移动端无需新建渲染器，仅新建一个路由 + 公共入口分流（4.3/4.4）。
- 按钮入口四处置（4.5）：右下角 FAB（`add`）、卡片操作区（行级）、勾选态底部操作栏（批量）、页面顶部（其余全局，与已实现的第四期工具条统筹）。
- 拆分：第六期按钮入口与体系骨架 → 第七期流程与批量动作 → 第八期表单/列表路由页（渲染入口已确认，剩余为路由页壳与联调）。

### 4.2 按钮动作处理口径矩阵（openType / btnType）

| 动作 | 桌面机制（来源） | 移动端口径 | 移动端现状 | 归属 |
| --- | --- | --- | --- | --- |
| 自定义 `fn` | `executeButton` 直接执行 | 与桌面一致 | ✅ 第二期已生效 | 六期回归 |
| `openType=1` 当前页跳转 | `disposeThisPageJump`（sessionStorage 传参 + prjId 切换 + `$router.push`） | 与桌面一致 | ✅ 第二期已实现 | 六期回归 |
| `openType=3` 新窗口 | `executeButton` 内 `window.open`（URL + 勾选参数） | 与桌面一致（H5 中为新标签页） | ✅ 第二期已实现 | 六期回归 |
| `openType=5` 接口请求 | `disposeRequestEvent`（confirm 前置 + `disposeParams` + `isRefresh` 重查） | 与桌面一致 | ✅ 第二期已实现 | 六期回归 |
| `openType=-1` `refresh` | `refresh()` | 与桌面一致 | ✅ 已有 | 六期回归 |
| `openType=-1` `batchDel` | `disposeDel` → `batchDel` → `requestBatchDel`（宿主 inject）成功后重查（`tableItem.js:2043/2461`） | 与桌面一致（属“调用接口”类）：`requestBatchDel` 由 H5 宿主 provide，移动端补 inject 与同语义实现（删除成功提示、末页删空回退页码、重查） | ❌ 告警占位 | 七期 |
| `openType=2` 流程 | `disposeFlowEvent`：check 先 `/flow/business/{mainFieldValue}` 查实例；发起/审批 `queryFlowDef` 查定义；`openFlow`（宿主）/ `stdNew` 新窗口（`tableItem.js:1621-1686`） | 跳转流程 H5 摘要页 `#/flowH5Summary`，参数与数据来源同桌面（4.6） | ❌ 告警占位 | 七期 |
| `openType=0` 动态表单 | 弹窗 + `VFRuntime` 表单运行时（`tableItem.js:2141-2185`），底部确定 → 运行时 `submitForm()` | 宿主路由页公共入口 `renderType=form` → `vFRender_view.vue`（4.4/4.3） | ❌ 告警占位 | 八期 |
| `openType=6` 动态列表 | 弹窗 + `nestedTable`（内嵌 `complete-table`，`tableItem.js:2188-2196`） | 宿主路由页公共入口 `renderType=pageList` → `tableRender.vue`（provide `isMobile` 自动切 `mobileTable`，4.4） | ❌ 告警占位 | 八期 |
| `openType=4` 关联组件 | `disposeRelateCompEvent` → `expose_showDialog` / 本地组件 `expose_showDialog` | **定性暂不实施**（依赖桌面弹窗与本地组件体系，H5 无对应承载），维持开发告警 | ❌ 告警占位 | 不实施 |
| `openType=-1` 下载/导入/二维码族（`download`/`flowDocDownload`/`flowResultDownload`/`formDownload`/`import`/`importRefresh`/`qrCode`） | 宿主能力 | 维持暂不实施（渲染方案 §4.2） | ❌ 告警占位 | 不实施 |

### 4.3 表单“确定”如何调接口、渲染入口确认（原未确认问题，2026-09-18 已确认）

**桌面机制（语义基准）**：弹窗底部“确定” → `submitForm()`（`tableItem.js:2529`）→ 调**表单运行时组件自身的 `submitForm()`**——校验与调用表单保存接口由运行时内部完成，列表组件不感知保存 URL；提交成功运行时触发 `submit` 事件 → 列表 `onSubmit(data)`（`tableItem.js:774`）执行 `isRefresh` 重查并关闭弹窗。仅 VForm 子表（`isVformWidget`，从表本地数据）场景由列表显式传 save URL（`create-slave`/`update-slave`），移动端无该场景不实施。查看态（`onlyRead`）以 `isDisabled` 渲染、不出现确定按钮。

**渲染入口确认（gate 解除）**：移动端表单渲染入口为宿主 `commondp-web/src/CommonDP/pages/mainMg/lowcode/component/vFRender_view.vue`，**自带 H5 适配，直接复用、无需新建渲染器**：

- 内嵌 `v-form-render` 运行时；`isMobile()`（UA 判定）为真时经 `convertPcFormToMobile` 将 PC 栅格表单平铺为移动端布局。
- `submitForm()` 即“确定调接口”：内部校验（`startValidate`，失败定位首个错误项滚动至可视区并提示）后按新增/编辑分别请求 `/dyn-common/save/${formId}`、`/dyn-common/update/${formId}`（`params.id = primaryKeyValue`），成功提示“保存成功”并 `$emit("submit", resData)`。
- 数据回显：`primaryKeyValue` 自行走 `dynamicDetail` 加载；`isDisabled` 只读（对应 `check`）；`hasSubmit=false` 时用外部按钮触发提交。
- 新增初始化与勾选传参：attrs `callingFrom="dynatic-table"` + `dlgFormConfig.btnType === "add"` 时 `externalParams`/`dynamicExternalParams` 合入初始 formData（与桌面 `getExternalCompBaseAttrs` 同构，表单侧以隐藏字段容器承接）；`selectList`/`paramsRule` attrs 支持勾选数据随表单提交（`submitRequest` 内处理 `deliverySelectListFields`/`paramName`）。

列表侧无此问题：`openType=6` 的渲染入口为宿主 `commonRender/components/tableRender.vue`（即 `complete-table` 的宿主壳，provide `isMobile: isMobile()`，H5 下自动切换为本仓库 `mobileTable`）。第八期剩余工作为宿主路由页壳、公共入口分流与 H5 场景联调回归。

### 4.4 表单/列表专用路由页（`openType=0/6`）设计

**承载方式（2026-09-18 确认）**：宿主 H5 侧**新建一个路由** + **仿 `commondp-web/src/CommonDP/pages/commonRender/index.vue` 建立一个公共入口组件**，由参数 `renderType` 控制具体渲染哪一个渲染器（两者均为宿主既有组件，直接复用）：

| `renderType` | 渲染器（宿主既有） | 说明 |
| --- | --- | --- |
| `pageList` | `commonRender/components/tableRender.vue` | `complete-table` 的宿主壳：内部 provide `isMobile: isMobile()`（UA 判定），H5 下自动切换为本仓库 `mobileTable` 卡片列表；勾选、第四期筛选工具条、详情页能力随组件自带。传 `relateId = tableId`，列表配置由其内部 `requestTableConfig` 按 id 拉取（等价桌面 `nestedTable` 不传 json 的路径），`init(false, null, { externalParams })` |
| `form` | `mainMg/lowcode/component/vFRender_view.vue` | 表单运行时入口，H5 适配与 `submitForm` 机制见 4.3 |

与 `commonRender/index.vue` 的差异：公共入口只借其“按参数分流渲染器”的形态；其配置获取走动态路由 meta（`type`/`relateId`）+ `getListConfigJSONByFormCode` 按 formCode 反查，而移动端入口由列表组件跳入、`formId`/`tableId` 来自按钮配置直传，建议 **query 驱动的静态路由**（如 `#/lowcodeModal`），不走菜单 meta 反查链路。

- 传参（业务参数走 sessionStorage、query 只带轻量标识，避免 URL 超长，与桌面 `lowcodeTableThisPageJumpParams` 传参风格一致）：
  - `renderType=pageList|form`；`id`：`formId`（`relateFrom`）/ `tableId`（`relateTable`）；
  - `primaryKeyValue`（编辑/查看回填主键）、`onlyRead`（check 只读）、`title`（`dialogTitle`，缺省按 `add`/`edit`/`check` 取 新增/编辑/查看）；
  - `externalParams`：`formatSelectListParams` 组装结果（表单 = 默认口径即桌面 `externalParamsFormRow`；列表 = `useArray` 口径，对齐 `disposeDynamicTableEvent`）；
  - `isRefresh` 标记（提交成功后回列表重查）。
- `renderType=form` 页面结构：导航返回 + 标题 + `vFRender_view`（`formId` + `primaryKeyValue` + `isDisabled = onlyRead`）+ 底部固定“确定/取消”（`onlyRead` 时不渲染确定）；确定 → `submitForm()`（校验 + `/dyn-common/save|update/${formId}`）→ `submit` 事件 → 路由返回 + `isRefresh` 重查；新增场景 `externalParams` 经 attrs 注入（`callingFrom="dynatic-table"` + `dlgFormConfig.btnType="add"`，桌面 `getExternalCompBaseAttrs` 同构）。
- `disposeDynamicFormEvent`/`disposeDynamicTableEvent` 移动端实现同桌面语义：`add`/`check`/`edit` 分支、主键与勾选校验提示（“请至少勾选一条要处理的数据！”“主键字段未取到值…”）与 `onlyRead`、默认标题。
- 返回机制：路由 back 回到列表；列表数据与滚动位置不丢（与第一期详情页同要求），仅 `isRefresh` 时重查（回刷机制与 4.6 流程跳转共用，第七期先行实现）。

### 4.5 按钮入口投放（“按钮放哪里”的确认口径）

对齐设计图 1 与渲染方案 §4.2/§4.4，四处置：

| 入口位置 | 承载按钮 | 说明 |
| --- | --- | --- |
| 右下角浮动按钮（FAB） | `add` 类全局按钮 | 设计图 1 明确的“+”；存在多个 `add` 类按钮时聚合为 FAB 点开的面板 |
| 卡片操作区（卡片底部一行） | 行级按钮：`edit`、`check`、行级自定义、被字段 `relateBtnId` 引用的按钮 | 常用 ≤3 个直出，其余收“···”半屏面板；字段片段点击入口第二期已有，不重复投放 |
| 勾选态底部操作栏 | 批量按钮：`batchDel` 及配置 `deliverySelectList` 的按钮 | 进入勾选态从底部滑出、取消勾选收起；选择数量校验复用 `validateSelectList` |
| 页面顶部工具条/右上“更多” | `refresh` 及其余无行数据依赖的全局按钮 | 与第四期已实现的顶部工具条同处顶部区域，视觉统筹（工具条右侧/第二行）在第六期实施时定稿 |

- 按钮可见性与校验链维持桌面顺序（渲染方案 §4.3：`extraOption.isHidden` → 权限过滤及 `authorize === "defaultShow"` → 预览/禁用/VForm/FreeLayout 场景过滤 → `deliverySelectList` 选择数量校验 → `validateFn`/`btnValidationOptions`/自定义 `fn` → `openType` 分派 → `isRefresh`）。
- 预览态/禁用态：入口渲染但置灰不可点（与卡片勾选框置灰口径一致）；预览态不执行动作、不发起跳转。
- `dbClickRelateBtnId`：定案移动端**不提供触发入口**（不发明长按等交互），执行链保留（经 `emitBtnClick` 可达）。

### 4.6 流程按钮（`openType=2`）跳转设计

- check（查看流程）：与桌面同源——`generalRequest(\`/flow/business/${mainFieldValue}\`)` 取流程实例（含 `flowInstanceId`/`currentVersionId`/`businessId` 等）；无 `flowInstanceId`（草稿态）提示“草稿状态的流程不能查看！”不跳转（对齐 `tableItem.js:1636`）。
- 发起/审批：与桌面同源——`queryFlowDef("", "", flowKey)`（宿主 inject，H5 宿主需 provide，移动端 inject 清单新增）取流程定义。
- 跳转目标：`${H5 基地址}/commonDph5.html#/flowH5Summary?currentVersionId=...&flowInstanceId=...&flowKey=...&approveType=...&businessId=...&enterpriseId=...&isProject=...`（以 2026-09-18 样例 URL 为基准）；`enterpriseId` 取宿主 inject。
- 待与流程 H5 确认（第七期开工前）：`approveType` 取值集合与各按钮场景映射（桌面 check 用 `view`、发起用 `add`，样例为 `edit`，以流程 H5 页面实际支持为准）；发起场景桌面 `startMode === "stdNew"` 走新窗口 `/examine-new`，H5 侧对应入口。
- 跳转方式：同宿主 hash 路由内跳转（`flowH5Summary` 注册于同 router 时用 `$router.push`，否则 `location.href`）。
- `isRefresh` 回刷：跳转前记录标记（组件级 pendingRefresh 或 sessionStorage），返回列表路由时触发 `loadFirst` 重查；该机制第七期实现，第八期表单/列表路由页复用。

### 4.7 六/七/八期拆分、依赖与不并入事项

| 期次 | 主题 | 范围 | 依赖/风险 |
| --- | --- | --- | --- |
| 第六期 | 按钮入口与体系骨架 | 4.5 四处入口 UI 与投放推导（渲染方案 §4.4 作用范围推导：`add`/`refresh` 全局、`batchDel`/`deliverySelectList` 批量、`edit`/`check` 及被 `relateBtnId` 引用为行级、其余按是否读取行字段分流）；可见性与场景过滤；预览/禁用置灰；`dbClickRelateBtnId` 定案、`openType=4` 定性暂不实施、`expose_showDialog`/`expose_hideDialog` 维持占位告警；已实现动作回归矩阵（`fn`/`1`/`3`/`5`/`-1` `refresh`） | 无外部依赖；顶部入口与第四期工具条的视觉统筹需出稿确认 |
| 第七期 | 流程与批量动作 | `openType=2` 跳转 `flowH5Summary`（4.6）；`isRefresh` 返回回刷机制；批量操作闭环（底部操作栏联动、`batchDel` 同桌面 `requestBatchDel` + 重查、`deliverySelectList` 传参、无勾选回退当前行的行按钮语义） | 移动端列表运行于宿主 `tableRender.vue` 内，其 provide 已含 `queryFlowDef`/`requestBatchDel`/`openFlow`（移动端仅补 inject 声明）；`approveType` 映射与流程 H5 对齐 |
| 第八期 | 表单/列表专用路由页 | 宿主新建一个路由 + 仿 `commonRender/index.vue` 公共入口、`renderType=pageList\|form` 分流（4.4，渲染器均为宿主既有组件直接复用）；底部确定/取消调接口链路（4.3）、编辑回填与只读、`externalParams` 注入、返回重查 | **渲染入口已确认、gate 解除（2026-09-18）**：表单 `vFRender_view.vue`（H5 适配已有）、列表 `tableRender.vue`（provide `isMobile` 自动切换）；剩余为路由页壳开发与 H5 场景联调回归 |

不并入本次三期、维持原状的事项：原第五期的复杂布局族（`tabs-table`、`tree-table`、多级表头分组、汇总、二维码、打印导出、导入、插槽/关联自定义组件、`isMerge`/`spanMethod`）维持暂不实施，后续按需另立期次。

### 4.8 明确不处理清单（按钮体系最终口径）

- `openType=4` 关联组件（本轮定性）：暂不实施，触发时维持开发告警。
- `openType=-1` 下载族（`download`/`flowDocDownload`/`flowResultDownload`/`formDownload`）、导入族（`import`/`importRefresh`）、`qrCode`：暂不实施（渲染方案 §4.2 维持）。
- `dbClickRelateBtnId` 触发入口：不提供（4.5 定案）。
- `expose_showDialog`/`expose_hideDialog`：表单/列表路由化、关联组件不实施后无承载场景，维持占位告警；`openType=0/6` 不再借道弹窗 API。
- VForm 子表本地数据场景（`isVformWidget` 的 `submitFormByVformWidget`/`submitLocalData`/`batchDelByVformWidget`/`localDataDel`）：移动端无 vform 从表宿主场景，不实施。
- 不渲染入口的按钮同样不进入权限/场景过滤计算，不改 JSON 与桌面端行为。

### 4.9 第六期实现记录（已实现，2026-09-18）

第六期范围（4.7）已全部落地于 `packages/mobileTable`，本节记录实现口径与本期定稿的决策；语义基准（4.5 投放、渲染方案 §4.3 可见性顺序、§4.4 作用范围推导）不变。

**投放推导（`btnPlacement` computed，渲染方案 §4.4）**：

- 对权限过滤后的 `btnList` 逐按钮推导四组：`add` → FAB；`refresh` → 顶部；`batchDel` 或 `deliverySelectList` → 批量；`edit`/`check` 及被字段片段 `clickEvent.relateBtnId` 引用 → 行级；其余 → 顶部。优先级：排除暂不实施 > `add` > `refresh` > 批量 > 行级 > 顶部。
- "其余按钮按参数映射判断是否读取行字段"的兜底统一归顶部全局，依据：现有动作类型中读取行字段的仅 `edit`/`check` 与被 `relateBtnId` 引用者——其余动作的参数映射上下文为查询参数（`getParams`，经 `convertDynaticData`）或勾选集合（`deliverySelectList`，经 `formatSelectListParams`），均不直接读行字段；自定义 `fn` 无法静态分析，归顶部（执行时 `fn(rowData)` 收到入口传入的行数据，与桌面全局按钮收到 `undefined` 的差异仅在于行级/批量入口会传行/勾选）。
- 被引用按钮 id 集合（`relatedBtnIds`）遍历 `tableConfigJSON` 全部叶子字段的 `contentTextAttrArr`（历史字符串配置经 `str2obj` 兼容解析，失败按无引用处理）。
- 暂不实施族（下载/导入/`qrCode` 七个 `btnType` 与 `openType=4`）不生成入口，但**保留在 `btnList` 中**——`emitBtnClick`（字段片段引用等）仍可达，触发时走既有的第五期告警占位，满足 4.8"不改 JSON 与桌面端行为"。

**可见性与场景过滤（渲染方案 §4.3 顺序，第六期实施部分）**：

1. `extraOption.isHidden`：不渲染入口（桌面 `display: none` 的等价物；不解析桌面附加的 `style` 字符串）。
2. 权限过滤与 `defaultShow`：沿用第二期 `composeBtnList`，本期补 FreeLayout 场景分支（与桌面 `composeBtnRegularOptions` 同口径）：`renderStrategy.source === "freeLayoutWidget"` 时仅 `btnVerifyType === "checkPermission"` 才过滤；VForm 子表场景移动端不实施（无 `getWidget` 宿主），不做其按钮显示筛选。
3. 预览态/禁用态：四处入口渲染但统一置灰不可点（`isBtnDisabled = previewMode || tableDisbaled || tagAttrs.disabled`，与卡片勾选框置灰口径一致；不采用桌面预览态放行 `add`/`edit`/`check` 的分支——移动端弹窗承载不实施，放行无意义）；`tagAttrs.disabled` 保留按钮自身禁用语义，`expose_enableAllBtn` 可恢复。
4. `deliverySelectList` 选择数量校验（`validateSelectList`）与 `validateFn`/`btnValidationOptions`/`fn`：不在入口层重复实施，由共享 `executeButton` 统一执行（顺序与桌面完全一致）；行级入口传入 `rowData` 时不因勾选为空被阻断（执行器内 `!row` 分支既有语义）。

**四处入口 UI（4.5 定稿，顶部视觉统筹本期定案为"第二行"）**：

- **页面顶部（`mt-btnbar`）**：定稿放第四期工具条**下方第二行**（工具条不渲染时独立成行常驻组件顶部），横排、超出横向滚动；承载 `refresh` 与其余全局按钮。按钮经 `el-button` 渲染，文字/图标/`type`/`plain`/`round` 取 `tagAttrs` 透传，尺寸固定 `small`。
- **右下角 FAB（`mt-fab`）**：固定组件右下角的"+"；单个 `add` 直接触发，多个 `add` 点开底部半屏面板聚合选择；勾选态批量栏滑出期间隐藏（避免遮挡）；层级低于第四期弹层蒙层（弹层展开时被压暗不可点）。
- **卡片操作区（`mt-card-actions`）**：卡片底部一行，行级按钮常用前 3 个直出（`edit`/`check` 在前，其后按 `formOptions` 顺序），其余收"···"底部半屏面板；整行点击阻止冒泡不触发卡片点击（打开详情）。
- **勾选态底部批量操作栏（`mt-batchbar`，本期骨架）**：`isShowCheckbox` 开启、存在批量按钮且有勾选时自底部滑入，显示"已选 N 项"+ 批量按钮 + "取消"（清空选择并对外发布 `selectionChange`）；取消勾选自动收起。按钮点击走 `executeButton`（无勾选时 `validateSelectList` 拦截提示）；`batchDel` 等批量动作闭环（同桌面 `requestBatchDel` 补实现）属第七期。
- 底部半屏面板（`mt-asheet`）为 FAB 聚合与卡片"···"共用形态：全屏蒙层 + 底部滑入 + 安全区适配，按钮行（图标 + 文字）逐个置灰，执行后关闭；与第四期顶部下拉弹层**同屏互斥**（打开任一侧关闭另一侧）；层级低于详情页。

**定性收口（4.5/4.8 本期落地）**：`dbClickRelateBtnId` 不提供触发入口（执行链经 `emitBtnClick` 保留，未改动）；`openType=4` 与下载/导入/二维码族不渲染入口、触发时告警；`expose_showDialog`/`expose_hideDialog` 维持占位告警未动。

**回归矩阵（已实现动作经新入口可达）**：自定义 `fn`（顶部入口，收到行数据）、`openType=1`（行级链路：sessionStorage 传参 + `queryChangePrjId` + `push`；批量入口无勾选被 `validateSelectList` 拦截）、`openType=3`（批量入口带勾选参数 `window.open`）、`openType=5`（顶部入口 `generalRequest`）、`openType=-1` `refresh`（顶部入口重查）——冒烟自测 30 项断言全部通过（构建产物 + jsdom 挂载，脚本 `.zcode/smoke-phase6.js`），webpack 全量构建通过。

**遗留（移交第七/八期）**：`openType=2` 流程跳转与 `isRefresh` 返回回刷、`batchDel` 同桌面补实现、底部操作栏与批量动作联动细节（第七期）；`openType=0/6` 路由页与表单确定调接口链路（第八期）。本期入口对这些动作照常渲染，点击触发现有告警占位。
