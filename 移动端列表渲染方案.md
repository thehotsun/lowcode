# lowcode 移动端列表渲染方案

> 本方案已按《移动端列表五期整体方案.md》微调：新增与 `tableAttrs` 平级的顶层移动端配置 `mobileAttrs`（设计/渲染双侧按默认值 merge，前向兼容）；卡片内展开模型改为点击卡片打开详情页；搜索移至第四期、按钮移至第五期、选择移至第三期。分期与验收以五期方案为准，本文各能力域的语义定义仍有效。
>
> **实施状态**：第一期已实现（2026-09-10），完成项与遗留问题见《移动端列表第一期实现契约.md》§12；第二期（字段交互）已实现（2026-09-14），见《移动端列表五期整体方案.md》1.6 第二期补齐与契约 §13；第三期（选择与集成对齐）已实现（2026-09-15），见《移动端列表五期整体方案.md》1.4 第三期补齐与契约 §14；第四期（搜索、筛选、排序）已实现（2026-09-18），见本文 §5 各节“第四期实现口径”注记与契约 §15。

## 1. 目标与边界

低代码列表移动端基于当前已保存的列表 JSON 运行，新增与 `tableAttrs` 平级的顶层移动端配置 `mobileAttrs`（标题键、移动端展示列、卡片单/双列、label 上下/左右布局）。设计端加载与渲染端解析均按 `merge({}, getMobileAttrs(), 保存值 || {})` 合并：旧 JSON 无该键取默认值，未来新增配置字段旧页面自动补默认值，不失效、不迁移，不影响桌面端。

移动端不复用桌面 `el-table` UI，而是将同一份 JSON 解释为单列卡片列表 + 详情页：

- 卡片展示标题键与移动端展示列摘要字段。
- 点击卡片通过 JS 在当前父级容器内铺满打开详情页，label 左右展示全部显示字段。
- 数据、权限、查询参数、生命周期和外部调用语义保持一致。
- 第一期覆盖配置解析、卡片与详情页渲染、格式化渲染（普通字段、字典、人员、内容文本样式、复杂 `formatter`）、加载更多与并发治理（详见 §10）。
- 搜索区域第四期，操作列与按钮第五期，第一期均不渲染入口。

当前配置由 `packages/setupTable/components/table-widget/index.vue` 组装，核心结构如下：

```json
{
  "pageLayout": "table",
  "tableOptions": [],
  "formOptions": [],
  "tableAttrs": {},
  "fuzzyFieldSearchConfig": {
    "searchFieldList": [],
    "placeholder": ""
  },
  "keyField": ""
}
```

## 2. 移动端页面结构

```text
[ 卡片列表（组件渲染范围；顶部导航栏由宿主页面提供） ]

------------------------------------------
标题键字段值                              <- mobileAttrs.titleField，未配置则本行不展示
label一            label二               <- labelLayout: vertical 时 label 上值下
值一               值二                    fieldLayout: double 时两列网格
label三            label四
值三               值四
------------------------------------------

[ 加载中 / 没有更多数据 / 加载更多 ]

点击卡片后（JS 控制，非路由跳转）：

------------------------------------------
←  详情                              <- 详情页铺满列表父级容器
标题键字段值（大标题）
label一 ···························· 值一  <- 全部显示字段，label 左右
label二 ···························· 值二
           ←   1/N   →                  <- 上一条/下一条
------------------------------------------
```

卡片态和详情页是同一记录的两种视图。打开详情页不重新请求详情数据，直接使用当前行数据。

## 3. 卡片字段规则

### 3.1 字段可见性

移动端与桌面端保持严格一致：只渲染 `Boolean(item.show) === true` 的字段。不能使用 `show !== false`，因为历史 JSON 可能缺少该字段，而桌面端实际使用 `filter(item => item.show)`。

移动端第一期不提供桌面端 `filterField` 会话级列设置面板，始终以保存配置中的 `show` 为准。该决定不回写 JSON，也不影响桌面端会话状态。

只将叶子字段用于卡片字段渲染。含 `children` 的父表头不作为字段渲染；多级表头分组属于后期（第五期）能力。

卡片展示列 = `mobileAttrs.mobileFields` 与 `show` 为真值的叶子列的交集；`mobileFields` 为空（或过滤失效项与标题键后无剩余）时回退前 4 个显示叶子列（已用作标题的剔除；不足 4 个有几个展示几个）。配置协议与回退规则见《移动端列表五期整体方案.md》1.3。

### 3.2 卡片摘要态

卡片字段由 `mobileAttrs` 决定：

- 标题行：`titleField`（标题键）单独一行渲染；从显示叶子列中配置，不再重复出现在字段区；未配置标题键时标题行不展示，不回退其他字段。
- 字段区：按 `mobileFields` 顺序渲染，`fieldLayout` 决定单列或两列网格（两列时先行后列排布），`labelLayout` 决定每个字段是 label 上/值在下还是 label 左/值右。
- 未进入卡片的显示字段不丢失，在详情页完整展示。
- 文本过长时截断，详情页显示完整值。
- `align` 不控制移动端字段值对齐：移动端不消费列 `align`（web 表格单元格语义），值固定居左，只受 `labelLayout` 布局影响。
- `columnWidth`、`fixed` 不控制卡片布局。
- `show-overflow-tooltip` 转换为“摘要截断、详情完整展示”。

旧 JSON 无 `mobileAttrs` 时按 3.1 的回退规则渲染。

### 3.3 详情页和行交互

- 展示全部 `show` 为真值的叶子字段，字段名使用 `fieldName`，label 左、值右布局，值超长换行完整展示。
- 详情页通过 JS 控制，以绝对定位铺满列表父级容器（父容器需有确定高度；未设置 `position: relative` 时组件自动补设），不走路由；返回后列表数据与滚动位置不变。
- 详情页底部 pager 在当前已加载记录内切换上一条/下一条；到达已加载末条且还有更多数据时自动请求下一页并进入新记录；打开详情页本身不触发详情接口。
- 列表数据被首查/刷新整体替换时，详情页当前下标越界即自动关闭。
- 默认点击卡片打开详情页，并同步当前行状态、发布 `rowClick`。
- 字段点击行为（`contentTextAttrArr.clickEvent`）已于第二期接入：配置了关联按钮的片段优先于卡片点击并阻止冒泡；`dbClickRelateBtnId` 关联动作的触发入口随第五期按钮体系确定。
- 当前行、选中行是两个独立状态，不能互相替代。

## 4. 按钮兼容方案

> 本章能力整体调整至第五期实施，第一期不渲染任何按钮入口（含新增、刷新、浮动按钮）。以下规则保留为按钮体系的语义定义。
>
> **2026-09-18 口径确认**：按钮体系逐类动作口径已确认并拆分为第六/七/八期实施——流程（`openType=2`）跳转流程 H5 摘要页 `#/flowH5Summary`；动态表单/列表（`openType=0/6`）经宿主新建的一个路由 + 仿 `commonRender/index.vue` 的公共入口按 `renderType=pageList|form` 分流渲染（form → 宿主 `mainMg/lowcode/component/vFRender_view.vue`，自带 H5 适配与 `submitForm()` 调接口链路；pageList → `commonRender/components/tableRender.vue`，provide `isMobile` 自动切移动端列表）；接口请求（`openType=5`）、当前页跳转（`openType=1`）、新窗口（`openType=3`）与桌面一致且第二期已实现；`batchDel` 属接口类同桌面补实现；关联组件（`openType=4`）与下载/导入/二维码族定性暂不实施。入口投放、渲染入口确认结论与拆分细节见《移动端列表五期整体方案.md》第四部分；本章 §4.2/§4.3/§4.4 仍是六/七/八期的语义基准。
>
> **2026-09-18 第六期已实现**：§4.2 投放位置与 §4.4 作用范围推导已按四处入口（顶部第二行按钮行 / 右下角 FAB / 卡片操作区（≤3 直出 + "···"半屏面板）/ 勾选态底部批量栏骨架）与 §4.3 可见性链第一至三步（`isHidden`、权限与 `defaultShow`、FreeLayout 场景与预览/禁用置灰）落地；实现口径与决策见《移动端列表五期整体方案.md》4.9。第四至六步（选择数量校验、行数据不阻断、`validateFn` 链）由共享 `executeButton` 统一执行，不在入口层重复。

### 4.1 当前按钮模型

列表没有独立持久化的“操作列”配置：

- `formOptions` 是列表全部功能按钮的统一配置池。
- `contentTextAttrArr.clickEvent.relateBtnId` 可将字段文本/图标关联到 `formOptions` 中的按钮。
- 移动端按全局、行级、批量三种作用范围投放按钮，不增加操作列配置。

### 4.2 按钮投放

| 按钮类别 | 移动端位置 | 第五期处理 |
| --- | --- | --- |
| `add` | 页面顶部右侧 | 支持。 |
| `refresh` | 页面顶部 | 支持。 |
| `batchDel` 或其他批量按钮 | 选择模式底部操作栏 | 支持选择校验和跨页选择。 |
| `edit`、`check` | 卡片（行操作区） | 传入当前 `rowData`。 |
| 行级自定义按钮 | 卡片“更多操作” | 传入当前 `rowData`。 |
| 字段点击关联按钮 | 字段文本/图标 | 传入当前 `rowData`。 |
| 无行数据依赖的全局按钮 | 页面顶部“更多操作” | 支持，但必须经过权限和隐藏判断。 |
| `import`、`importRefresh` | 不展示 | 第五期暂不实施。 |
| `download`、`flowDocDownload`、`formDownload` | 不展示 | 第五期暂不实施。 |
| `qrCode` | 不展示 | 第五期暂不实施。 |
| 关联自定义组件 | 不展示 | 第五期暂不实施。 |

### 4.3 按钮可见性和校验

按钮处理顺序必须与桌面端一致：

1. 应用 `extraOption.isHidden`。
2. 应用权限过滤及 `authorize === "defaultShow"` 规则。
3. 应用预览态、页面禁用态和 VForm/FreeLayout 场景过滤。
4. 只有 `deliverySelectList` 为真时才执行 `validate` 选择数量校验。
5. 行按钮传入 `rowData` 时，不因当前勾选为空而错误阻断行操作。
6. 执行 `validateFn`、`btnValidationOptions`、自定义 `fn`。
7. 按 `openType` 执行业务动作。
8. 按 `isRefresh` 决定是否刷新列表。

### 4.4 按钮作用范围推导

- `add`、`refresh`：全局按钮。
- `batchDel` 或配置 `deliverySelectList`：批量按钮。
- `edit`、`check`：行按钮。
- 被字段 `relateBtnId` 引用的按钮：行按钮。
- 其余按钮根据参数映射判断是否读取当前行字段；读取行字段的放卡片，否则放页面“更多操作”。
- 暂不实施按钮不渲染入口，不修改 JSON 和桌面端行为。

移动端复用桌面按钮执行器的业务分派，不复制一套业务逻辑。共享能力 `executeButton(btnConfig, rowData, host)`（`host` 为宿主列表实例）已于第二期抽取落地（`packages/completeTable/component/executeButton.js`，桌面 `handleBtnClick` 改薄壳、行为不变）。

## 5. 搜索、表头筛选和排序

> 本章能力已于第四期实现（2026-09-18，交互形态与实现决策见《移动端列表五期整体方案.md》1.8“第四期补齐”）。筛选不区分表头筛选与搜索控件位置（above / below-header），统一收纳为搜索框（右侧固定“搜索”按钮）下方的「排序/全部筛选」两按钮 + 下拉弹层（自列表区顶部下探；排序弹层点选即生效；筛选弹层为分组表单、确认制草稿、表单区滚动而底部按钮固定）。以下各节规则保留为语义定义，各节内注明第四期实现口径。

### 5.1 搜索控件

仅当字段同时满足有效 `searchWidget` 和 `isSearchWidget === true` 时创建搜索条件。移动端将普通搜索条件、`position === "below-header"` 的表头下搜索条件和表头列筛选统一收纳到“全部筛选”下拉弹层（每个有效筛选字段一个分组）；不再保留桌面表头位置，但不能丢失字段配置。

第四期需保持以下语义（均已实现）：

- `searchWidget` 决定控件类型。
- `searchWidgetConfig.tagAttrs` 保持控件属性。
- `request.require`、`request.url`、`request.type`、`request.params` 保持远程选项请求语义。
- `request.autoFillOptions` 保持自动填充逻辑。
- `extraOption.props.key`、`label`、`children` 保持下拉和级联取值映射。
- 字典按 `dicCode` 请求，支持 `labelTranslateType`、`sortNum`、字典结果缓存和 `previewMode` 禁止请求。
- `listeners.change` 仍以列表实例作为 `this`；触发顺序与桌面 `addEventListener` 包装一致：先触发查询、再执行已有联动回调；移动端筛选确认不能吞掉该回调。
- 数值范围不完整时不提交不完整的范围查询；日期范围拆成 `${field}Start` 和 `${field}End`。
- 弹层“确认”后整体写回并重置页码查询；桌面“本地模式”（vform 从表）移动端无对应场景，不实现本地筛选副本。
- 搜索防抖：移动端确认均为显式触发（点选/确认/回车），无桌面 300ms 防抖需求，亦无防抖需在销毁时清理。

**第四期实现口径**：筛选承载为“全部筛选”下拉弹层的分组表单（组标题 = 字段名）；选项列表类（下拉/字典）为换行平铺 pill 按钮组（单选 + “全部”）、超 8 项组标题“展开/收起”；文本与数值范围为输入控件；日期/日期范围/级联按 `searchWidget` 类型渲染对应控件（`tagAttrs` 透传）；全部控件绑定草稿（`filterDraft`），底部“重置/确认”确认制——确认整体写回 `searchForm` 并重查、收起即放弃草稿；multiple 控件仍单选 UI 但提交单元素数组保持桌面请求形状；分组标题优先 `searchWidgetConfig.formItemAttrs.label`（桌面搜索区标签同来源），未配置回退 `fieldName`；按 `sortNumb` 升序排序。

`fuzzyFieldSearchConfig` 固定放在页面顶部，使用原 `searchFieldList` 和 `placeholder`（`searchFieldList` 非空才渲染）。输入即 trim 绑定 `multiFieldSearch`；右侧固定“搜索”按钮，点击（或回车）后才重置页码并查询，清空输入等同当前关键字为空时点击“搜索”。

### 5.2 表头筛选

这里的“表头筛选”指列 `filters`，不是 `renderHeader`：

- 移动端在“全部筛选”弹层中支持字段筛选（无搜索控件、仅列筛选来源的字段同样渲染分组，内容为选项 pill 按钮组单选 + “全部”）。
- 过滤来源优先级为 `filters` > `filtersConfig.customHandler` > `filtersConfig.isFilter`。
- `customHandler(tableData)` 在面板打开时对当前已加载数据重新生成筛选项（等价桌面 `tableDataChangeQueue` 的数据变化重算语义）。
- 自动筛选支持 `isSplit`、`splitChar`、非空去重。
- `limitShowWord`、`maxlength` 只截断显示文本，提交值保持原始值。
- 提交口径：选中值经 `searchForm` 同通道提交（与搜索控件同形状的等值条件）走替换式查询。桌面列筛选为 el-table 本地过滤（`filter-method`）、且“本地模式”仅存在于 vform 从表场景，移动端均无对应场景，不执行 `filter-method`、不发明独立的远端列筛选参数。

### 5.3 排序

> 本节原表述（“桌面端 `sort` 和 `sort-method` 是本地排序能力、`getParams()` 没有排序参数、第四期只承诺本地排序”）已过时：桌面提交 939194c（2026-09-03）已整体废弃 `sort-method`，全部 `sort: true` 列改为 `sortable: "custom"` 后端排序。第四期（2026-09-18）按桌面现状实现：

- 排序项在“排序”下拉弹层中选择：首行“默认排序”清除排序条件，其后每个可排序列一行；点选即生效并关闭，同字段再点在升/降间切换（清空排序统一走“默认排序”行）。
- 排序协议与桌面完全一致：升/降提交 `externalParams.sqlConfig = { sort: [{ field, order: "asc" | "desc" }] }`、取消提交 `{ sort: [] }`（桌面 `flowStatusSortMethod` + `refreshData` 同语义，经 `externalParams` 持久化，翻页与后续查询自动携带）。
- 不执行本地排序、不消费已废弃的 `sort-method` 配置，不发明 `sqlConfig` 之外的排序参数。

## 6. 字段渲染兼容

### 6.1 渲染能力覆盖矩阵

| 渲染能力 | 第一期处理 |
| --- | --- |
| 普通字段值 | 完整支持。 |
| 复杂自定义 `formatter` | 完整支持（验收口径见 6.2）。 |
| `contentTextAttrArr` | 第一期支持条件、文本、样式、图标；字段点击（配置 `clickEvent.relateBtnId` 的片段）已于第二期接入。 |
| `cellRenderType === PERSON` | 支持人员名称和首字母头像。 |
| `cellRenderType === DICT` | 支持字典映射、颜色和状态标签。 |
| `show` | 支持（显隐语义与桌面一致）。 |
| `align` | 不消费：列对齐是 web 表格单元格语义，移动端值固定居左。 |
| `show-overflow-tooltip` | 摘要截断、详情完整展示。 |
| 字段点击行为 | 已于第二期支持（配置 `clickEvent.relateBtnId` 的片段），优先于卡片点击并阻止冒泡；未配置的片段不拦截卡片点击。 |
| `renderHeader` | 不执行桌面表头脚本，使用 `fieldName` 作为标签。 |
| 关联自定义组件 | 第五期暂不实施。 |

字段渲染优先级必须与桌面一致：

`formatter` > `contentTextAttrArr` > `PERSON` > `DICT` > 原始值。

`formatter` 分派见 `BaseRenderTable/index.js:798-810`，其余见 `getCellRender`（`:191-264`）。桌面代码中另有 `slotName` 插槽、`tagName` 程序化组件渲染两层，因设计器保存的 JSON 不会产出这两个字段，移动端渲染链中省略。各层互斥，命中即止。

格式化渲染由字段配置驱动：有 `contentTextAttrArr`、`cellRenderType` 等配置才启用对应渲染，无任何格式化配置的字段展示纯文本（原始值）。该规则对卡片字段区、卡片标题行、详情页标题区与字段行一致——凡有格式化配置的位置一律优先格式化渲染。

### 6.2 `formatter`

第一期必须支持复杂自定义 `formatter`，通过移动端字段渲染上下文执行：

```js
renderMobileField({ row, column, index, mode }) // mode: "card" | "detail"
```

提供：

```js
getTableRenderInstance()
emitBtnClick(row, btnName, btnId)
```

上下文含 `mobileMode: true`（`mode` 取值 `"card" | "detail"` 见上方签名）。

支持 formatter 返回的组件定义及其 `components`、`data`、`computed`、`watch`、`methods`、`template`。不为依赖 Element Table 专有 API 的历史 formatter 提供兼容代理（2026-09-11 决策）：此类脚本异常时输出开发告警并回退该字段后续渲染链路（内容文本/字典/人员/原始值）。复杂 formatter 验收口径：渲染机制与 web 端 `BaseRenderTable` 一致即视为满足，真实环境逐个回归随宿主联调进行，不作为交付阻塞项。

### 6.3 `contentTextAttrArr`

- `conditionalJudgment` 以列表实例为 `this` 执行，异常时该片段不展示。
- `textVal || cellValue` 保持桌面语义；`textVal` 支持 `{}` 占位符模板解析（与桌面端一致）：`{}` 取当前单元格的值，`{fieldName}` 取行数据中对应字段的值，未匹配的字段替换为空串（`{fieldName}` 按键名精确匹配行数据，不做大小写回退）。
- `isBold`、`isItalic`、`isStrikethrough`、`isUnderline`、`color`、`textStyle`、`iconName`、`iconPosition`、`iconStyle` 保持生效。
- `clickEvent.relateBtnId` 通过统一按钮执行器触发，传入当前行（第二期已生效：配置了关联按钮的片段可点击，可执行动作子集与限制见五期方案 1.6 第二期补齐）。
- 片段点击阻止冒泡（第二期已生效；仅配置了关联按钮的片段绑定点击，未配置的片段不拦截卡片点击）。

### 6.4 字典和人员

- 字典配置只有 `dicCode` 时，移动端先请求 `/dic/content/list?dicCode=...`。
- 使用 `dicId === String(cellValue)` 匹配。
- 未匹配时回退原始值。
- 解析字典项 `contentStyle` 的 `backgroundColor` 和 `color`。
- 请求失败时保留原始值并输出错误日志。
- 人员类型保持名称展示和首字母头像语义。

### 6.5 `renderHeader`

移动端没有表格表头，第一期不执行依赖 `h`、`column`、`$index` 或 Element Table VNode 的 `renderHeader`。卡片字段标签、筛选项标签统一使用 `fieldName`。这是明确的产品降级，不伪造桌面表头上下文。

## 7. 选择、当前行与外部事件

> 选择能力与 FreeLayout 事件全集已于第三期对齐（2026-09-15）；第一期仅维护 `currentSelectedRow`（卡片点击时更新）并发布 `rowClick`。

选择能力实施时必须同时维护：

- `currentSelectedRow`：当前操作行；列表数据被整体替换后（替换式查询或 `expose_setTableData`）按 `keyField` 重锚：记录仍在新数据中则保留选中并换为新对象，已不存在则置 `null`（`keyField` 缺失时置 `null`）。
- `selectList`：当前选择集合。
- `keyField`：记录唯一身份；需兼容返回字段大小写差异。

选择规则（第三期已实现）：

- `isShowCheckbox` 控制是否在卡片左侧渲染勾选框；勾选阻止冒泡、不触发卡片点击打开详情页；预览态/禁用态置灰不可交互。
- 单选模式只保留最后选择项（`renderStrategy.singleSelect`，与桌面同来源）。
- 批量操作使用 `selectList`；无勾选时，允许按桌面语义回退当前行的行按钮（第五期按钮体系消费，`getSelectedData` 已按该语义实现）。
- 跨页选择（`renderStrategy.crossPageSelect`，与桌面 `reserve-selection` 同来源）开启时，替换式查询（重新搜索、筛选、重置、外部参数改变、刷新、`expose_setTableData`）后按 `keyField` 重锚选择集合：仍存在的记录保留（换为新对象），已不存在的移除；未开启跨页选择时替换后清空（对齐桌面 `reserve-selection=false`）。
- 加载更多追加数据时已选状态保留，不重复添加相同主键（选择同一性按 `keyField` 判定，缺失时退化为对象身份）。
- `keyField` 缺失或记录无主键时不纳入跨页保留集合，并输出开发告警。

兼容父组件和 FreeLayout 事件（第三期已实现）：

- 父组件：`selectListHandler`、`rowClick`（`complete-table` 均向宿主转发）。
- FreeLayout：`${widgetId}.selectionChange`、`${widgetId}.rowClick`、`${widgetId}.currentChange`（卡片点击且当前行实际变化时发布）；`${widgetId}.rowDblclick` 移动端无双击交互入口、不绑定触发。
- 程序性维护选择集合（替换后的重锚/清空）不发布选择事件，避免替换式查询级联触发业务事件。
- 预览态和禁用态统一阻止业务事件与按钮执行。
- 卡片点击打开详情页不自动等同于双击；`dbClickRelateBtnId` 执行链已随第二期 `emitBtnClick` 生效而就绪，但移动端无双击交互入口，触发形态待第五期按钮体系确定。

## 8. 外部 API 与生命周期

> 第一期提供 `init` 与最小 expose 集合（`expose_preview`、`expose_refreshData`、`expose_getTableData`、`expose_setTableData`）；全集已于第三期对齐（`expose_showDialog`/`expose_hideDialog` 依赖第五期弹窗体系，现阶段为占位告警）。

移动端组件对外保持桌面组件 API 名称和主要参数：

- `init(isPreview, json, options, externalTriggerQueryTableData, tableDisbaled)`。
- `expose_setSearchForm(data, multiFieldSearch)`。
- `expose_refreshData(data, dynamicData)`。
- `expose_setTableData(data)`、`expose_getTableData()`。
- `expose_setTableDisbaled(bool)`。
- `expose_enableAllBtn()`。
- `expose_CompleteTableInstance()`。
- `expose_showDialog()`、`expose_hideDialog()`，按移动端弹层承载实现。
- `expose_preview(data)`，生成预览数据但不请求字典和远程选项。

外部参数语义：`data` 合并到持久化 `externalParams`，`dynamicData` 替换动态外部参数；`expose_setSearchForm` 重置搜索表单后写入传入值并从第一页查询。

生命周期时序：

1. 解析配置并组合运行数据。
2. 执行 `onInitEvent`。
3. 读取 sessionStorage 跳转参数（键 `lowcodeTableThisPageJumpParams`），为对象时合并进 `externalParams` 并随即删除（预览态不执行）。
4. 组装最终查询参数。
5. 执行 `onBeforeQueryDataEvent(params)`；同步返回 `false` 时取消本次请求。
6. 请求成功后执行 `onAfterQueryDataEvent(params, res.data)`。
7. 首查替换列表，加载更多追加列表。
8. `onResetBtnEvent` 存在时由其完全接管默认重置逻辑。
9. 预览态（`init` 的 `isPreview` 或 `expose_preview`）不执行生命周期钩子、不发起查询，仅以本地样例数据渲染。

## 9. 分页与加载更多

同时支持触底加载和手动按钮：

```text
加载中
没有更多数据
[ 加载更多 ]
```

- `showPagination === true` 时使用既有 `pageNo`、`paginationSize`。
- 首次加载、搜索、筛选、重置、外部参数变更从第一页开始并替换数据；失败保留已有数据，页码按已加载数据反推并提示。
- 触底加载递增页码并追加数据。
- `showPagination === false` 保持一次性加载语义。
- `totalCount` 用于判断是否还有数据。
- 同一时间只允许一个加载更多请求；重复触底直接忽略。
- 首查/筛选/刷新使用请求版本号，旧响应不得覆盖新查询（含被抢占的加载更多不回写页码）。
- 加载更多失败时恢复页码，保留已有卡片并允许重试。
- 组件销毁后禁止异步结果回写。
- 搜索防抖在销毁时取消。

## 10. 实施范围

分期总表与验收标准以《移动端列表五期整体方案.md》为准，概要如下。

### 第一期：配置 + 卡片 + 详情页（本期实施）

- `pageLayout: "table"`。
- 设计端顶层 `mobileAttrs`（标题键、展示列、单双列、label 布局），设计/渲染双侧 merge 默认值前向兼容。
- 单列卡片列表：标题行 + 单/双列字段网格 + label 上下/左右。
- 点击卡片 JS 铺满父级容器的详情页：全部显示字段、label 左右、上一条/下一条 pager（末条自动加载下一页）。
- 普通字段、字典、人员、`contentTextAttrArr` 样式、复杂自定义 `formatter` 等格式化渲染。
- 分页、触底加载、手动加载更多、刷新和并发治理（`requestVersion`）。
- 严格按 `Boolean(show)` 处理列显隐；`renderHeader` 降级为 `fieldName` 标签。
- 最小外部 API（`init`、`expose_preview`、`expose_refreshData`、`expose_getTableData`、`expose_setTableData`）与 `rowClick`。

### 第二期：字段交互（已实现，2026-09-14）

- 字段点击行为与按钮执行器共享抽取 `executeButton(btnConfig, rowData, host)`，使 `emitBtnClick` 完整生效。

### 第三期：选择与集成对齐（已实现，2026-09-15）

- 卡片选择、单选/多选、跨页选择（`keyField` 去重与替换式查询重锚）、选择校验宿主能力（`getSelectedData` 等）。
- `selectListHandler` 与 FreeLayout 事件（`selectionChange`/`rowClick`/`currentChange`；`rowDblclick` 无双击交互入口）。
- `expose_*` API 全集（`expose_showDialog`/`expose_hideDialog` 为第五期占位告警）、预览态/禁用态细化（`tableDisbaledStatus` 与 `dynamicExternalParams` 联动）、设计器内移动端预览（预览弹窗桌面/移动端切换，手机壳内 provide `isMobile`）。

### 第四期：搜索、筛选、排序（已实现，2026-09-18）

- 搜索框（右侧“搜索”按钮，点击才调接口）+「排序/全部筛选」左右两按钮：排序打开下拉弹层选项列表（点选即生效，升/降互切、清空走“默认排序”），全部筛选打开下拉弹层分组表单（pill 按钮组 + 控件 + 重置/确认，确认制草稿；表单区超出弹层高度内部滚动、底部按钮固定）；弹层自列表区顶部下探、蒙层压暗列表区；`onResetBtnEvent` 经筛选弹层“重置”接入；排序对齐桌面 `sqlConfig` 后端协议，本地排序与本地模式不实现（移动端无对应场景）。实现口径见《移动端列表五期整体方案.md》1.8 第四期补齐。

### 第五期：按钮与复杂布局（按钮体系已拆分为六/七/八期，复杂布局暂不考虑）

- 按钮体系（操作列与按钮的全局/行级/批量投放、权限、`openType` 分派、批量操作）已于 2026-09-18 确认口径并拆分为第六/七/八期实施（见上方 §4 头部注记与《移动端列表五期整体方案.md》第四部分）。
- `tabs-table`、`tree-table`、多级表头分组、汇总、二维码、打印导出、导入、插槽/关联自定义组件、`isMerge`/`spanMethod`、移动端会话级列设置面板：维持暂不实施。

### 第六期：按钮入口与体系骨架（已实现，2026-09-18）

- 四处按钮入口 UI 已落地：右下角 FAB（`add`，单个直触发、多个点开底部半屏面板聚合，勾选态隐藏）/ 卡片操作区（行级常用 ≤3 直出 + "···"底部半屏面板）/ 勾选态底部批量操作栏（骨架：已选计数 + 批量按钮 + 取消，`batchDel` 动作闭环属第七期）/ 页面顶部第二行按钮行（`refresh` 及其余全局按钮，与第四期工具条统筹定稿为"工具条下方第二行"）。
- 按钮可见性与场景过滤：`isHidden` 不渲染入口、权限过滤及 `defaultShow` 沿用并补 FreeLayout `btnVerifyType` 分支、预览/禁用统一置灰；`deliverySelectList` 校验与 `validateFn` 链由共享 `executeButton` 统一执行。
- 投放推导（§4.4）：`add`→FAB、`refresh`→顶部、`batchDel`/`deliverySelectList`→批量、`edit`/`check`/被 `relateBtnId` 引用→行级、其余→顶部（现有动作的参数映射上下文为查询参数或勾选集合，不直接读行字段）。
- `dbClickRelateBtnId` 不提供触发入口、`openType=4` 定性暂不实施、`expose_showDialog`/`expose_hideDialog` 维持占位告警；暂不实施族不渲染入口但保留在 `btnList`（`emitBtnClick` 可达、触发时告警）。
- 已实现动作（`fn`/`openType` `1`/`3`/`5`/`-1` `refresh`）回归矩阵经新入口自测通过（30/30）。实现口径与决策见《移动端列表五期整体方案.md》4.9。

### 第七期：流程与批量动作（已实现，2026-09-20）

- `openType=2` 流程跳转流程 H5 摘要页 `#/flowH5Summary`：check 查流程实例（`/flow/business/{主键值}`，草稿态提示不跳转）、发起/审批经 `queryFlowDef`（宿主 inject，本期补声明）查流程定义；跳转参数白名单取自样例 URL（`currentVersionId`/`flowInstanceId`/`flowKey`/`businessId`/`approveType`/`enterpriseId`/`isProject`），`approveType` 按桌面映射（check `view`、发起/审批 `add`，取值集合待流程 H5 联调确认）；`stdNew` 发起走同宿主 `#/examine-new`（桌面新窗口 `/examine-new` 的等价入口）；同 router 注册时 `$router.push`、否则 `location.href` 拼 H5 基地址。
- `isRefresh` 返回回刷：跳转前 sessionStorage 记标记（按 `listPageId` 匹配），keep-alive 返回经 `activated` 消费触发 `loadFirst`，非 keep-alive 重挂载由 init 首查覆盖（init 内清理标记防重复刷新）；第八期路由页复用同一机制。
- 批量操作闭环：`disposeDel`/`batchDel` 与桌面同语义（行数据直删 / 勾选集合删除 / 主键缺失提示；`requestBatchDel` 宿主 inject 本期补声明，成功提示后 `loadFirst` 重查），底部操作栏经共享 `executeButton` 联动 `validateSelectList`，`deliverySelectList` 传参沿用既有 `formatSelectListParams` 链路，无勾选回退当前行（`getSelectedData`/`getFirstSelectedData`）。实现口径与决策见《移动端列表五期整体方案.md》4.10。

### 第八期：表单/列表专用路由页（待实施，渲染入口已确认）

- 宿主 H5 新建一个路由 + 仿 `commonRender/index.vue` 的公共入口，query `renderType=pageList|form` 分流：`form` → 宿主表单渲染入口 `mainMg/lowcode/component/vFRender_view.vue`（H5 适配已有：`isMobile()` 时 `convertPcFormToMobile` 平铺；底部“确定/取消”→ 其自带 `submitForm()`，校验 + `/dyn-common/save|update/${formId}`）；`pageList` → `commonRender/components/tableRender.vue`（provide `isMobile` 自动切 `mobileTable`，`externalParams` 注入对齐桌面 `nestedTable`）。编辑回填（`primaryKeyValue`）与只读、`isRefresh` 返回重查。原“宿主 VForm 运行时 H5 可用性”gate 已解除（2026-09-18 确认）。

## 11. 关键结论

1. 移动端消费现有 JSON，并新增顶层配置 `mobileAttrs`（标题键、展示列、单双列、label 布局，独立于 `tableAttrs`）；设计/渲染双侧按默认值 merge，旧 JSON 走回退规则，不迁移、不影响桌面端。
2. `tableOptions` 决定字段渲染能力与 `show` 显隐；卡片展示字段由 `mobileAttrs` 决定；卡片内展开模型调整为点击卡片打开详情页。
3. `formOptions` 是统一按钮池，运行时划分为全局、行级和批量按钮；按钮体系整体后移至第五期。
4. `tableAttrs` 负责分页、生命周期、数据转换、选择和事件语义；复杂树、汇总能力按分期实施。
5. `show` 使用桌面同样的真值语义；不提供移动端会话列设置。
6. `renderHeader` 明确降级为 `fieldName`；格式化渲染（含复杂 `formatter`）第一期完整支持。
7. 排序采用桌面既有 `sqlConfig` 后端排序协议（提交 939194c 后桌面已废弃 `sort-method`；原“桌面无远端排序参数”表述已过时）；除该协议与 `searchForm` 通道外，不得自行发明其他远端排序、远端筛选参数。
8. 外部 API、FreeLayout 事件、当前行和选择集合按第三期对齐为完整运行时契约。
9. 五期划分与验收标准以《移动端列表五期整体方案.md》为准。
